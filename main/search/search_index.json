{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#overview","title":"Overview","text":""},{"location":"#api-stability","title":"API stability","text":"<p> While <code>metriclab</code> is in development stage, no API is guaranteed to be stable from one release to the next. In fact, it is very likely that the API will change multiple times before a stable 1.0.0 release. In practice, this means that upgrading <code>metriclab</code> to a new version will possibly break any code that was using the old version of <code>metriclab</code>.</p>"},{"location":"#license","title":"License","text":"<p><code>metriclab</code> is licensed under BSD 3-Clause \"New\" or \"Revised\" license available in LICENSE file.</p>"},{"location":"get_started/","title":"Get Started","text":"<p>It is highly recommended to install in a virtual environment to keep your system in order.</p>"},{"location":"get_started/#installing-with-uv-recommended","title":"Installing with <code>uv</code> (recommended)","text":"<p>The following command installs the latest stable version of the library:</p> <pre><code>uv pip install metriclab\n</code></pre> <p>To install the latest development version from GitHub:</p> <pre><code>uv pip install git+https://github.com/durandtibo/metriclab.git\n</code></pre> <p>To install a specific version:</p> <pre><code>uv pip install metriclab==0.3.0\n</code></pre>"},{"location":"get_started/#installing-with-pip","title":"Installing with <code>pip</code>","text":"<p>The following command installs the latest stable version of the library:</p> <pre><code>pip install metriclab\n</code></pre> <p>To install the latest development version from GitHub:</p> <pre><code>pip install git+https://github.com/durandtibo/metriclab.git\n</code></pre> <p>To install a specific version:</p> <pre><code>pip install metriclab==0.3.0\n</code></pre>"},{"location":"get_started/#verifying-installation","title":"Verifying Installation","text":"<p>After installation, you can verify that metriclab is correctly installed by running:</p> <pre><code>python -c \"import metriclab; print(metriclab.__version__)\"\n</code></pre> <p>Or try a simple example:</p> <pre><code>from metriclab.functional.array import accuracy\n\nresult = accuracy(y_true=[1, 0, 1, 1], y_pred=[1, 1, 1, 0])\nprint(result.accuracy)  # Output: 0.5\n</code></pre>"},{"location":"get_started/#installing-from-source","title":"Installing from source","text":"<p>To install <code>metriclab</code> from source, you can follow the steps below.</p>"},{"location":"get_started/#prerequisites","title":"Prerequisites","text":"<p>This project uses <code>uv</code> for dependency management. Please refer to the uv installation documentation for installation instructions.</p>"},{"location":"get_started/#clone-the-repository","title":"Clone the Repository","text":"<pre><code>git clone git@github.com:durandtibo/metriclab.git\ncd metriclab\n</code></pre>"},{"location":"get_started/#create-a-virtual-environment","title":"Create a Virtual Environment","text":"<p>It is recommended to create a Python 3.10+ virtual environment:</p> <pre><code>make setup-venv\n</code></pre> <p>This command creates a virtual environment using <code>uv</code> and installs all dependencies including development tools.</p> <p>Alternatively, you can create a conda virtual environment:</p> <pre><code>make conda\nconda activate metriclab\nmake install\n</code></pre>"},{"location":"get_started/#install-dependencies","title":"Install Dependencies","text":"<p>To install only the core dependencies:</p> <pre><code>make install\n</code></pre> <p>To install all dependencies including documentation tools:</p> <pre><code>make install-all\n</code></pre>"},{"location":"get_started/#verify-installation","title":"Verify Installation","text":"<p>You can test the installation with the following command:</p> <pre><code>make unit-test-cov\n</code></pre> <p>This will run the test suite with coverage reporting.</p>"},{"location":"get_started/#development-setup","title":"Development Setup","text":"<p>If you plan to contribute to metriclab, please also install the development tools.</p> <p>Using <code>uv</code>:</p> <pre><code>uv pip install -e \".[dev,docs]\"\n</code></pre> <p>Using <code>pip</code>:</p> <pre><code>pip install -e \".[dev,docs]\"\n</code></pre> <p>Then install the pre-commit hooks:</p> <pre><code>pre-commit install\n</code></pre> <p>See CONTRIBUTING.md for more information about contributing.</p>"},{"location":"dev/contributing/","title":"Contributing to <code>metriclab</code>","text":"<p>We want to make contributing to this project as easy and transparent as possible.</p>"},{"location":"dev/contributing/#overview","title":"Overview","text":"<p>We welcome contributions from anyone, even if you are new to open source.</p> <ul> <li>If you are planning to contribute back bug-fixes, please do so without any further discussion.</li> <li>If you plan to contribute new features, utility functions, or extensions to the core, please first   open an issue and discuss the feature with us.</li> </ul> <p>Once you implement and test your feature or bug-fix, please submit a Pull Request.</p>"},{"location":"dev/contributing/#development-setup","title":"Development Setup","text":""},{"location":"dev/contributing/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10 or later</li> <li>Git</li> <li><code>uv</code> for dependency management (recommended) or <code>pip</code></li> </ul>"},{"location":"dev/contributing/#setting-up-your-development-environment","title":"Setting up your development environment","text":"<ol> <li>Fork the repository on GitHub</li> <li> <p>Clone your fork locally:    <pre><code>git clone git@github.com:YOUR_USERNAME/metriclab.git\ncd metriclab\n</code></pre></p> </li> <li> <p>Set up the development environment:    <pre><code>make setup-venv\n</code></pre></p> </li> </ol> <p>This will create a virtual environment and install all dependencies.</p> <ol> <li> <p>Activate the virtual environment:    <pre><code>source .venv/bin/activate\n</code></pre></p> </li> <li> <p>Install pre-commit hooks:    <pre><code>pre-commit install\n</code></pre></p> </li> </ol>"},{"location":"dev/contributing/#pull-requests","title":"Pull Requests","text":"<p>We actively welcome your pull requests.</p> <ol> <li>Fork the repo and create your branch from <code>main</code>.</li> <li>Make your changes with clear, descriptive commit messages.</li> <li>If you've added code that should be tested, add tests.</li> <li>If you've changed APIs, update the documentation.</li> <li>Ensure the test suite passes:    <pre><code>make unit-test-cov\n</code></pre></li> <li>Make sure your code lints:    <pre><code>inv check-lint\n</code></pre></li> <li>Format your code:    <pre><code>inv check-format\n</code></pre></li> <li>Run the pre-commit hooks:    <pre><code>pre-commit run --all-files\n</code></pre></li> <li>Push to your fork and submit a pull request to the <code>main</code> branch.</li> </ol>"},{"location":"dev/contributing/#pull-request-guidelines","title":"Pull Request Guidelines","text":"<ul> <li>Keep pull requests focused on a single issue or feature</li> <li>Write clear, descriptive commit messages</li> <li>Include tests for new functionality</li> <li>Update documentation as needed</li> <li>Ensure all CI checks pass</li> <li>Reference any related issues in the PR description</li> </ul>"},{"location":"dev/contributing/#code-style","title":"Code Style","text":"<p>This project follows these style guidelines:</p> <ul> <li>Code formatting: Black with 100 character line length</li> <li>Linting: Ruff</li> <li>Docstrings   : Google style</li> <li>Type hints: Required for all public APIs</li> </ul> <p>Pre-commit hooks will automatically check formatting and run linters.</p>"},{"location":"dev/contributing/#testing","title":"Testing","text":""},{"location":"dev/contributing/#running-tests","title":"Running Tests","text":"<p>Run all tests:</p> <pre><code>inv unit-test --cov\n</code></pre>"},{"location":"dev/contributing/#writing-tests","title":"Writing Tests","text":"<ul> <li>Place tests in the <code>tests/</code> directory</li> <li>Use descriptive test names that explain what is being tested</li> <li>Follow the existing test structure and patterns</li> <li>Aim for high code coverage</li> <li>Test edge cases and error conditions</li> </ul>"},{"location":"dev/contributing/#documentation","title":"Documentation","text":""},{"location":"dev/contributing/#building-documentation","title":"Building Documentation","text":"<p>Build the documentation locally:</p> <pre><code>cd docs\nmike serve\n</code></pre> <p>Then open http://localhost:8000 in your browser.</p>"},{"location":"dev/contributing/#documentation-guidelines","title":"Documentation Guidelines","text":"<ul> <li>Update docstrings for any changed functions/classes</li> <li>Add examples to docstrings when helpful</li> <li>Update user guide pages for API changes</li> <li>Keep documentation clear and concise</li> </ul>"},{"location":"dev/contributing/#issues","title":"Issues","text":"<p>We use GitHub issues to track public bugs or feature requests.</p>"},{"location":"dev/contributing/#reporting-bugs","title":"Reporting Bugs","text":"<p>When reporting bugs, please include:</p> <ul> <li>A clear, descriptive title</li> <li>Steps to reproduce the issue</li> <li>Expected behavior vs actual behavior</li> <li>Python version and metriclab version</li> <li>Minimal code example that demonstrates the issue</li> <li>Error messages and stack traces (if applicable)</li> </ul>"},{"location":"dev/contributing/#feature-requests","title":"Feature Requests","text":"<p>For feature requests, please include:</p> <ul> <li>A clear, concise description of the feature</li> <li>Motivation: why is this feature needed?</li> <li>Use case examples</li> <li>Proposed API or implementation (if applicable)</li> <li>Any alternatives you've considered</li> </ul>"},{"location":"dev/contributing/#code-review-process","title":"Code Review Process","text":"<ol> <li>A maintainer will review your PR</li> <li>You may be asked to make changes</li> <li>Once approved, a maintainer will merge your PR</li> </ol> <p>Please be patient - maintainers review PRs as time permits.</p>"},{"location":"dev/contributing/#community-guidelines","title":"Community Guidelines","text":"<ul> <li>Be respectful and constructive</li> <li>Welcome newcomers and help them get started</li> <li>Focus on the code, not the person</li> <li>Assume good intentions</li> </ul> <p>We follow the Code of Conduct.</p>"},{"location":"dev/contributing/#release-process","title":"Release Process","text":"<p>Releases are handled by maintainers:</p> <ol> <li>Update version in <code>pyproject.toml</code></li> <li>Create a git tag</li> <li>Publish to PyPI</li> <li>Update documentation</li> </ol>"},{"location":"dev/contributing/#getting-help","title":"Getting Help","text":"<ul> <li>Check the FAQ</li> <li>Browse existing issues</li> <li>Open a discussion</li> </ul>"},{"location":"dev/contributing/#license","title":"License","text":"<p>By contributing to <code>metriclab</code>, you agree that your contributions will be licensed under the LICENSE file in the root directory of this source tree.</p>"},{"location":"dev/github_workflows/","title":"GitHub Actions Workflows","text":"<p>This document provides an overview of the GitHub Actions workflows and the structure of the <code>.github</code> folder in the <code>metriclab</code> repository.</p>"},{"location":"dev/github_workflows/#overview","title":"Overview","text":"<p>The repository uses GitHub Actions for continuous integration, testing, documentation deployment, and package publishing. The workflows are designed to be modular and reusable, with a custom composite action for setting up the development environment.</p>"},{"location":"dev/github_workflows/#github-folder-structure","title":"<code>.github</code> Folder Structure","text":"<pre><code>.github/\n\u251c\u2500\u2500 actions/                   # Custom composite actions\n\u2502   \u2514\u2500\u2500 setup-env/             # Reusable action for environment setup\n\u2502       \u2514\u2500\u2500 action.yaml\n\u251c\u2500\u2500 workflows/                 # GitHub Actions workflow definitions\n\u2502   \u251c\u2500\u2500 build.yaml             # Build and package tests\n\u2502   \u251c\u2500\u2500 ci.yaml                # Main CI workflow\n\u2502   \u251c\u2500\u2500 coverage.yaml          # Code coverage reporting\n\u2502   \u251c\u2500\u2500 test.yaml              # Unit and integration tests\n\u2502   \u251c\u2500\u2500 docs.yaml              # Documentation deployment\n\u2502   \u251c\u2500\u2500 pypi.yaml              # PyPI package publishing\n\u2502   \u2514\u2500\u2500 ...                    # Additional workflows\n\u251c\u2500\u2500 CONTRIBUTING.md            # Contribution guidelines\n\u251c\u2500\u2500 ISSUE_TEMPLATE/            # Issue templates\n\u2502   \u251c\u2500\u2500 bug-report.yml\n\u2502   \u2514\u2500\u2500 feature-request.yml\n\u251c\u2500\u2500 PULL_REQUEST_TEMPLATE.md   # Pull request template\n\u2514\u2500\u2500 dependabot.yml             # Dependabot configuration\n</code></pre>"},{"location":"dev/github_workflows/#custom-composite-action","title":"Custom Composite Action","text":""},{"location":"dev/github_workflows/#setup-env","title":"<code>setup-env</code>","text":"<p>Location: <code>.github/actions/setup-env/action.yaml</code></p> <p>A reusable composite action that handles the common setup steps for most workflows.</p> <p>Purpose:</p> <ul> <li>Installs <code>uv</code> package manager</li> <li>Sets up Python environment</li> <li>Installs dependencies using invoke tasks</li> <li>Shows environment configuration and dependency tree</li> </ul> <p>Inputs:</p> <ul> <li><code>python-version</code>: Python version to use (default: <code>3.13</code>)</li> <li><code>package-name</code>: Optional package name to install</li> <li><code>package-version</code>: Optional package version to install</li> <li><code>install-args</code>: Optional arguments when installing dependencies (e.g., <code>--docs-deps</code>,   <code>--no-optional-deps</code>)</li> </ul> <p>Usage:</p> <pre><code>- name: Setup python environment\n  uses: ./.github/actions/setup-env\n  with:\n    python-version: '3.12'\n    install-args: '--docs-deps'\n</code></pre>"},{"location":"dev/github_workflows/#main-workflows","title":"Main Workflows","text":""},{"location":"dev/github_workflows/#ci-workflow-ciyaml","title":"CI Workflow (<code>ci.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Pull requests to <code>main</code> branch</li> <li>Pushes to <code>main</code> branch</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Main continuous integration workflow that orchestrates all quality checks.</p> <p>Jobs: Calls the following reusable workflows:</p> <ul> <li><code>build</code> - Package build tests</li> <li><code>coverage</code> - Code coverage reporting</li> <li><code>cyclic-imports</code> - Cyclic import detection</li> <li><code>doctest</code> - Documentation tests</li> <li><code>format</code> - Code formatting checks</li> <li><code>pre-commit</code> - Pre-commit hook validation</li> <li><code>test</code> - Unit and integration tests</li> <li><code>test-deps</code> - Dependency compatibility tests</li> </ul>"},{"location":"dev/github_workflows/#build-tests-buildyaml","title":"Build Tests (<code>build.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Called by other workflows (<code>workflow_call</code>)</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Validates that the package can be built and installed correctly across different distribution types and configurations.</p> <p>Jobs:</p> <ol> <li>build:<ul> <li>Uses a matrix strategy to test:<ul> <li>Distribution types: <code>sdist</code> (source distribution) and <code>wheel</code></li> <li>Extras: base installation (no extras) and <code>all</code> optional dependencies</li> </ul> </li> <li>Builds the package using <code>uv build</code></li> <li>Checks that <code>py.typed</code> is present in the distribution</li> <li>Installs the built package (with or without extras)</li> <li>Validates the build with <code>twine check</code></li> <li>Checks package metadata</li> <li>Checks dependency tree</li> <li>Verifies basic import works</li> <li>Validates package version is not <code>0.0.0</code></li> <li>Verifies that <code>pyright</code> recognizes the package as typed via type checking</li> </ul> </li> </ol>"},{"location":"dev/github_workflows/#test-workflow-testyaml","title":"Test Workflow (<code>test.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Called by other workflows (<code>workflow_call</code>)</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Runs comprehensive test suite across multiple Python versions and operating systems.</p> <p>Jobs:</p> <ol> <li> <p>all:</p> <ul> <li>Runs on multiple OS: Ubuntu (latest, 24.04, 22.04, ARM), macOS (latest, 26, 15, 14, Intel)</li> <li>Tests Python versions: 3.10, 3.11, 3.12, 3.13, 3.14 (including free-threaded builds)</li> <li>Runs unit and integration tests with coverage</li> </ul> </li> <li> <p>min:</p> <ul> <li>Same matrix as <code>all</code> but with minimal dependencies (<code>--no-optional-deps</code>)</li> <li>Ensures the package works without optional dependencies</li> </ul> </li> </ol>"},{"location":"dev/github_workflows/#coverage-workflow-coverageyaml","title":"Coverage Workflow (<code>coverage.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Called by other workflows (<code>workflow_call</code>)</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Generates code coverage reports and uploads them to Codecov.</p> <p>Jobs:</p> <ul> <li>Runs unit and integration tests with coverage</li> <li>Uploads coverage data to Codecov (only for the main repository)</li> <li>Failures in uploading to Codecov do not fail the workflow</li> </ul>"},{"location":"dev/github_workflows/#documentation-workflows","title":"Documentation Workflows","text":""},{"location":"dev/github_workflows/#stable-documentation-docsyaml","title":"Stable Documentation (<code>docs.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Version tags (<code>v*.*.*</code>)</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Builds and deploys documentation for stable releases.</p> <p>Jobs:</p> <ul> <li>Sets up environment with documentation dependencies</li> <li>Configures git for the docs bot</li> <li>Publishes documentation using <code>mike</code> (documentation versioning tool)</li> </ul>"},{"location":"dev/github_workflows/#development-documentation-docs-devyaml","title":"Development Documentation (<code>docs-dev.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Pushes to <code>main</code> branch</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Builds and deploys documentation for the development version.</p>"},{"location":"dev/github_workflows/#pypi-publishing-pypiyaml","title":"PyPI Publishing (<code>pypi.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Version tags (<code>v*.*.*</code>)</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Publishes the package to PyPI.</p> <p>Jobs:</p> <ul> <li>Installs <code>uv</code> and <code>invoke</code></li> <li>Runs the publish command with PyPI token from secrets</li> </ul>"},{"location":"dev/github_workflows/#additional-workflows","title":"Additional Workflows","text":""},{"location":"dev/github_workflows/#nightly-tests-nightly-testsyaml","title":"Nightly Tests (<code>nightly-tests.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Scheduled daily at 9:10 AM UTC</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Runs the full test suite nightly to catch issues early.</p>"},{"location":"dev/github_workflows/#nightly-package-tests-nightly-packageyaml","title":"Nightly Package Tests (<code>nightly-package.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Scheduled daily at 10:30 AM UTC</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Tests package installation and imports nightly.</p>"},{"location":"dev/github_workflows/#pre-commit-pre-commityaml","title":"Pre-commit (<code>pre-commit.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Called by other workflows</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Runs pre-commit hooks to validate code style and quality.</p>"},{"location":"dev/github_workflows/#pre-commit-auto-update-pre-commit-autoupdateyaml","title":"Pre-commit Auto-update (<code>pre-commit-autoupdate.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Scheduled weekly on Sundays at 5:10 AM UTC</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Automatically updates pre-commit hook versions.</p>"},{"location":"dev/github_workflows/#format-check-formatyaml","title":"Format Check (<code>format.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Called by other workflows</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Checks code formatting using Black and Ruff.</p>"},{"location":"dev/github_workflows/#cyclic-imports-cyclic-importsyaml","title":"Cyclic Imports (<code>cyclic-imports.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Called by other workflows</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Detects circular import dependencies.</p>"},{"location":"dev/github_workflows/#doctest-doctestyaml","title":"Doctest (<code>doctest.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Called by other workflows</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Runs doctests to validate code examples in documentation.</p>"},{"location":"dev/github_workflows/#test-dependencies-test-depsyaml","title":"Test Dependencies (<code>test-deps.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Called by other workflows</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Tests the package with different versions of dependencies.</p>"},{"location":"dev/github_workflows/#dependency-review-dependency-reviewyml","title":"Dependency Review (<code>dependency-review.yml</code>)","text":"<p>Triggers:</p> <ul> <li>Pull requests</li> </ul> <p>Purpose: Reviews dependency changes for security vulnerabilities.</p>"},{"location":"dev/github_workflows/#update-dependencies-update-depsyaml","title":"Update Dependencies (<code>update-deps.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Scheduled weekly on Sundays at 5:10 AM UTC</li> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Creates pull requests to update dependencies.</p>"},{"location":"dev/github_workflows/#cancel-stale-queued-runs-cancel_stale_queued_runsyaml","title":"Cancel Stale Queued Runs (<code>cancel_stale_queued_runs.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Push to any branch</li> <li>Pull requests</li> </ul> <p>Purpose: Cancels outdated workflow runs to save CI resources.</p>"},{"location":"dev/github_workflows/#generate-package-versions-generate-package-versionsyaml","title":"Generate Package Versions (<code>generate-package-versions.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Generates version compatibility documentation.</p>"},{"location":"dev/github_workflows/#get-versions-get-versionsyaml","title":"Get Versions (<code>get-versions.yaml</code>)","text":"<p>Triggers:</p> <ul> <li>Manual workflow dispatch</li> </ul> <p>Purpose: Retrieves version information from the project.</p>"},{"location":"dev/github_workflows/#workflow-permissions","title":"Workflow Permissions","text":"<p>Most workflows use minimal permissions following the principle of least privilege:</p> <ul> <li><code>contents: read</code> - Default for read-only operations</li> <li><code>contents: write</code> - Only for workflows that need to push changes (e.g., documentation deployment)</li> </ul>"},{"location":"dev/github_workflows/#secrets-used","title":"Secrets Used","text":"<p>The following secrets are configured in the repository settings:</p> <ul> <li><code>CODECOV_TOKEN</code> - For uploading coverage reports to Codecov</li> <li><code>PYPI_TOKEN</code> - For publishing packages to PyPI</li> <li><code>CI_BOT_EMAIL</code> - Email address for the CI bot (used for documentation deployment)</li> </ul>"},{"location":"dev/github_workflows/#best-practices","title":"Best Practices","text":"<ol> <li> <p>Reusable Workflows: Common workflows are defined with <code>workflow_call</code> trigger so they can be    reused by other workflows.</p> </li> <li> <p>Matrix Testing: The test workflows use matrix strategies to test across multiple Python    versions and operating systems.</p> </li> <li> <p>Composite Actions: The <code>setup-env</code> action centralizes common setup steps, reducing    duplication.</p> </li> <li> <p>Timeouts: All jobs have reasonable timeout limits (typically 10 minutes) to prevent hung    workflows.</p> </li> <li> <p>Fail-Fast: Most matrix jobs use <code>fail-fast: false</code> to continue testing other combinations    even if one fails.</p> </li> <li> <p>Version Pinning: GitHub Actions are pinned to specific major versions (e.g., <code>@v6</code>, <code>@v7</code>)    for stability.</p> </li> <li> <p>Minimal Changes: Workflows are designed to make minimal assumptions about the environment and    clean up after themselves.</p> </li> </ol>"},{"location":"dev/github_workflows/#dependabot-configuration","title":"Dependabot Configuration","text":"<p>The repository uses Dependabot to automatically update GitHub Actions dependencies. The configuration is in <code>.github/dependabot.yml</code>.</p>"},{"location":"dev/github_workflows/#issue-and-pull-request-templates","title":"Issue and Pull Request Templates","text":"<p>The repository provides templates to help contributors submit well-structured issues and pull requests:</p> <ul> <li>Bug Report: <code>.github/ISSUE_TEMPLATE/bug-report.yml</code></li> <li>Feature Request: <code>.github/ISSUE_TEMPLATE/feature-request.yml</code></li> <li>Pull Request Template: <code>.github/PULL_REQUEST_TEMPLATE.md</code></li> </ul>"},{"location":"dev/github_workflows/#troubleshooting-workflow-issues","title":"Troubleshooting Workflow Issues","text":""},{"location":"dev/github_workflows/#common-issues-and-solutions","title":"Common Issues and Solutions","text":""},{"location":"dev/github_workflows/#workflow-fails-with-permission-denied","title":"Workflow Fails with \"Permission denied\"","text":"<p>Cause: Insufficient permissions in the workflow configuration.</p> <p>Solution: Verify that the workflow has the necessary permissions. For example, workflows that push changes need <code>contents: write</code>.</p>"},{"location":"dev/github_workflows/#workflow-hangs-or-times-out","title":"Workflow Hangs or Times Out","text":"<p>Cause: A step is taking longer than expected or is waiting for input.</p> <p>Solution: - Check the timeout settings in the workflow (default is 10 minutes for most jobs) - Review job logs to identify which step is hanging - Consider increasing the timeout if the step legitimately needs more time</p>"},{"location":"dev/github_workflows/#matrix-job-failures","title":"Matrix Job Failures","text":"<p>Cause: One configuration in the matrix is failing while others pass.</p> <p>Solution: - Check the specific configuration that failed (Python version, OS, etc.) - Review the job logs for that specific matrix combination - Test locally with the same configuration if possible</p>"},{"location":"dev/github_workflows/#secret-not-available","title":"Secret Not Available","text":"<p>Cause: Required secret is not configured in repository settings.</p> <p>Solution: - Go to repository Settings &gt; Secrets and variables &gt; Actions - Add the required secret (e.g., <code>CODECOV_TOKEN</code>, <code>PYPI_TOKEN</code>)</p>"},{"location":"dev/github_workflows/#dependabot-pr-fails","title":"Dependabot PR Fails","text":"<p>Cause: Dependency update breaks tests or compatibility.</p> <p>Solution: - Review the Dependabot PR description for breaking changes - Check the failing test logs - Either fix the compatibility issue or close the PR if the update is not needed</p>"},{"location":"dev/github_workflows/#manually-triggering-workflows","title":"Manually Triggering Workflows","text":"<p>Most workflows can be manually triggered using <code>workflow_dispatch</code>:</p> <ol> <li>Go to the \"Actions\" tab in GitHub</li> <li>Select the workflow you want to run</li> <li>Click \"Run workflow\"</li> <li>Select the branch and any required inputs</li> <li>Click \"Run workflow\" button</li> </ol>"},{"location":"dev/github_workflows/#viewing-workflow-logs","title":"Viewing Workflow Logs","text":"<p>To view detailed logs for a workflow run:</p> <ol> <li>Go to the \"Actions\" tab</li> <li>Click on the workflow run</li> <li>Click on a specific job to see its steps</li> <li>Click on a step to expand its logs</li> <li>Use the search box to filter log output</li> </ol>"},{"location":"dev/github_workflows/#local-testing","title":"Local Testing","text":"<p>Before pushing changes, you can test workflows locally:</p> <pre><code># Run tests locally\ninv unit-test --cov\ninv integration-test --cov\n\n# Check code formatting\ninv check-format\n\n# Check types\ninv check-types\n\n# Run pre-commit hooks\npre-commit run --all-files\n\n# Check markdown documentation\n./dev/check_markdown.sh\n</code></pre>"},{"location":"dev/github_workflows/#workflow-usage-examples","title":"Workflow Usage Examples","text":""},{"location":"dev/github_workflows/#running-build-validation-locally","title":"Running Build Validation Locally","text":"<p>To replicate what the build workflow does:</p> <pre><code># Build source distribution\nuv build --sdist\n\n# Build wheel\nuv build --wheel\n\n# Install from built wheel\nuv pip install dist/metriclab-*.whl\n\n# Validate package metadata\n./dev/package/check_metadata.sh\n\n# Check dependency tree\n./dev/package/check_dependency_tree.sh\n\n# Validate type annotations\n./dev/package/check_type.sh\n\n# Run custom checks\n./dev/package/custom_checks.sh\n</code></pre>"},{"location":"dev/github_workflows/#testing-with-different-python-versions","title":"Testing with Different Python Versions","text":"<p>The test workflow tests multiple Python versions. To test locally with different versions:</p> <pre><code># Using pyenv to test with Python 3.11\npyenv install 3.11\npyenv local 3.11\npython -m venv .venv-3.11\nsource .venv-3.11/bin/activate\nuv pip install -e .\ninv unit-test\n\n# Test with minimal dependencies\nuv pip install -e . --no-optional-deps\ninv unit-test\n</code></pre>"},{"location":"dev/github_workflows/#maintenance-tasks","title":"Maintenance Tasks","text":""},{"location":"dev/github_workflows/#updating-pre-commit-hooks","title":"Updating Pre-commit Hooks","text":"<p>Happens automatically weekly via <code>pre-commit-autoupdate.yaml</code>, but can be done manually:</p> <pre><code>pre-commit autoupdate\npre-commit run --all-files\ngit commit -am \"Update pre-commit hooks\"\n</code></pre>"},{"location":"dev/github_workflows/#updating-dependencies","title":"Updating Dependencies","text":"<p>Happens automatically weekly via <code>update-deps.yaml</code>, but can be done manually:</p> <pre><code>inv update\ninv install --docs-deps\ninv unit-test --cov\n</code></pre>"},{"location":"dev/github_workflows/#regenerating-package-version-configuration","title":"Regenerating Package Version Configuration","text":"<p>Happens automatically weekly via <code>generate-package-versions.yaml</code>, but can be done manually:</p> <pre><code>python dev/generate_versions.py\ngit commit -am \"Generate package versions\"\n</code></pre>"},{"location":"dev/github_workflows/#contributing","title":"Contributing","text":"<p>For detailed information about contributing to the project, including how to run tests locally and development setup, see CONTRIBUTING.md.</p>"},{"location":"dev/security/","title":"Security Policy","text":""},{"location":"dev/security/#supported-versions","title":"Supported Versions","text":"<p>We release patches for security vulnerabilities. The following versions are currently supported:</p> Version Supported 0.1.x &lt; 0.1.0"},{"location":"dev/security/#reporting-a-vulnerability","title":"Reporting a Vulnerability","text":"<p>The <code>metriclab</code> team takes security bugs seriously. We appreciate your efforts to responsibly disclose your findings.</p>"},{"location":"dev/security/#where-to-report","title":"Where to Report","text":"<p>Please do not report security vulnerabilities through public GitHub issues.</p> <p>Instead, please report security vulnerabilities by emailing:</p> <p>durand.tibo+gh@gmail.com</p>"},{"location":"dev/security/#what-to-include","title":"What to Include","text":"<p>Please include the following information in your report:</p> <ul> <li>Type of issue (e.g., buffer overflow, SQL injection, cross-site scripting, etc.)</li> <li>Full paths of source file(s) related to the manifestation of the issue</li> <li>The location of the affected source code (tag/branch/commit or direct URL)</li> <li>Any special configuration required to reproduce the issue</li> <li>Step-by-step instructions to reproduce the issue</li> <li>Proof-of-concept or exploit code (if possible)</li> <li>Impact of the issue, including how an attacker might exploit the issue</li> </ul> <p>This information will help us triage your report more quickly.</p>"},{"location":"dev/security/#security-update-policy","title":"Security Update Policy","text":"<ul> <li>Security updates will be released as soon as possible</li> <li>Security updates will be clearly marked in release notes</li> <li>We will notify users through GitHub releases and other appropriate channels</li> </ul>"},{"location":"dev/security/#security-best-practices","title":"Security Best Practices","text":"<p>When using <code>metriclab</code> in your projects:</p> <ol> <li>Keep dependencies updated: Regularly update to the latest version of <code>metriclab</code> and its    dependencies</li> <li>Monitor security advisories: Watch the repository for security announcements</li> <li>Review code: When comparing sensitive data, ensure appropriate access controls are in place</li> <li>Validate inputs: Always validate data before comparison, especially when handling user inputs</li> </ol>"},{"location":"dev/security/#known-security-considerations","title":"Known Security Considerations","text":"<p><code>metriclab</code> is a library for comparing complex objects. When using it:</p> <ul> <li>Resource exhaustion: Be cautious when comparing very large or deeply nested data structures,   as this may consume significant memory and CPU resources</li> <li>Sensitive data: When comparing objects containing sensitive data, ensure proper access   controls and logging are in place</li> <li>Third-party data structures: When extending <code>metriclab</code> to support custom types, ensure proper   validation and error handling</li> </ul>"},{"location":"refs/","title":"Main classes and functions","text":""},{"location":"refs/#metriclab","title":"metriclab","text":"<p>Utilities and result containers for evaluating model predictions.</p> <p>The public API is currently centered around immutable result objects such as <code>metriclab.results.Result</code> and <code>metriclab.results.AccuracyResult</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import AccuracyResult\n&gt;&gt;&gt; AccuracyResult(num_correct_predictions=8, num_predictions=10).accuracy\n0.8\n</code></pre>"},{"location":"refs/exceptions/","title":"Exceptions","text":""},{"location":"refs/exceptions/#metriclab.exceptions","title":"metriclab.exceptions","text":"<p>Define the base exceptions.</p>"},{"location":"refs/exceptions/#metriclab.exceptions.EmptyMetricError","title":"metriclab.exceptions.EmptyMetricError","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when an empty metric is evaluated.</p> <p>This exception is raised by metric functions when there are no valid predictions to evaluate and <code>raise_empty=True</code> (the default).</p> Example <pre><code>&gt;&gt;&gt; from metriclab.exceptions import EmptyMetricError\n&gt;&gt;&gt; from metriclab.functional import accuracy\n&gt;&gt;&gt; try:\n...     accuracy(y_true=[], y_pred=[])\n... except EmptyMetricError as e:\n...     print(type(e).__name__)\n...\nEmptyMetricError\n</code></pre>"},{"location":"refs/functional/","title":"Functional","text":""},{"location":"refs/functional/#metriclab.functional","title":"metriclab.functional","text":"<p>Namespace for stateless metric functions.</p> <p>This package is reserved for functional APIs that compute metrics directly from inputs without storing intermediate state.</p>"},{"location":"refs/functional/#metriclab.functional.accuracy","title":"metriclab.functional.accuracy","text":"<pre><code>accuracy(\n    y_true: ArrayLike,\n    y_pred: ArrayLike,\n    *,\n    missing_policy: MissingPolicy = \"propagate\",\n    missing_values: Any = NOT_SET,\n    raise_empty: bool = True\n) -&gt; AccuracyResult\n</code></pre> <p>Compute the accuracy score.</p> <p>Parameters:</p> Name Type Description Default <code>y_true</code> <code>ArrayLike</code> <p>The ground truth target labels.</p> required <code>y_pred</code> <code>ArrayLike</code> <p>The predicted labels.</p> required <code>missing_policy</code> <code>MissingPolicy</code> <p>The policy for handling missing values. Valid values are <code>'omit'</code>, <code>'propagate'</code>, or <code>'raise'</code>. <code>'omit'</code> removes rows where any array equals <code>missing_values</code> before computing the metric. <code>'propagate'</code> keeps the rows but returns <code>num_correct_predictions=nan</code> when missing values are detected. <code>'raise'</code> raises a <code>ValueError</code> if any array contains <code>missing_values</code>.</p> <code>'propagate'</code> <code>missing_values</code> <code>Any</code> <p>The value to treat as missing. If not set, missing value handling is disabled regardless of <code>missing_policy</code>.</p> <code>NOT_SET</code> <code>raise_empty</code> <code>bool</code> <p>If <code>True</code>, raises <code>EmptyMetricError</code> when there are no valid predictions. If <code>False</code>, returns an <code>AccuracyResult</code> with <code>num_predictions=0</code>.</p> <code>True</code> <p>Returns:</p> Type Description <code>AccuracyResult</code> <p>The accuracy result. When missing values are present and <code>missing_policy='propagate'</code>, the result keeps <code>num_predictions</code> and sets <code>num_correct_predictions</code> to <code>nan</code>.</p> <p>Raises:</p> Type Description <code>EmptyMetricError</code> <p>if there are no valid predictions and <code>raise_empty</code> is <code>True</code>.</p> <code>ValueError</code> <p>if <code>missing_policy</code> is invalid.</p> <code>ValueError</code> <p>if <code>y_true</code> or <code>y_pred</code> contains <code>missing_values</code> and <code>missing_policy</code> is <code>'raise'</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.functional import accuracy\n&gt;&gt;&gt; # with numpy arrays\n&gt;&gt;&gt; accuracy(\n...     y_true=np.array([1, 0, 0, 1, 1]),\n...     y_pred=np.array([1, 0, 0, 1, 1]),\n... )\nAccuracyResult(num_correct_predictions=5, num_predictions=5)\n&gt;&gt;&gt; # with lists\n&gt;&gt;&gt; accuracy(y_true=[1, 0, 0, 1, 1], y_pred=[1, 0, 1, 1, 1])\nAccuracyResult(num_correct_predictions=4, num_predictions=5)\n&gt;&gt;&gt; # with string labels\n&gt;&gt;&gt; accuracy(\n...     y_true=[\"cat\", \"dog\", \"cat\", \"dog\"],\n...     y_pred=[\"cat\", \"dog\", \"dog\", \"dog\"],\n... )\nAccuracyResult(num_correct_predictions=3, num_predictions=4)\n&gt;&gt;&gt; # with missing values and missing_policy='propagate' (default)\n&gt;&gt;&gt; accuracy(\n...     y_true=np.array([1.0, 0.0, 0.0, 1.0, float(\"nan\")]),\n...     y_pred=np.array([1.0, 0.0, 0.0, 1.0, 1.0]),\n...     missing_values=float(\"nan\"),\n... )\nAccuracyResult(num_correct_predictions=nan, num_predictions=5)\n&gt;&gt;&gt; # with missing values and missing_policy='omit'\n&gt;&gt;&gt; accuracy(\n...     y_true=np.array([1.0, 0.0, 0.0, 1.0, float(\"nan\")]),\n...     y_pred=np.array([1.0, 0.0, 0.0, 1.0, 1.0]),\n...     missing_policy=\"omit\",\n...     missing_values=float(\"nan\"),\n... )\nAccuracyResult(num_correct_predictions=4, num_predictions=4)\n&gt;&gt;&gt; # allow empty result instead of raising\n&gt;&gt;&gt; accuracy(y_true=[], y_pred=[], raise_empty=False)\nAccuracyResult(num_correct_predictions=nan, num_predictions=0)\n</code></pre>"},{"location":"refs/functional/#metriclab.functional.balanced_accuracy","title":"metriclab.functional.balanced_accuracy","text":"<pre><code>balanced_accuracy(\n    y_true: ArrayLike,\n    y_pred: ArrayLike,\n    *,\n    missing_policy: MissingPolicy = \"propagate\",\n    missing_values: Any = NOT_SET,\n    raise_empty: bool = True\n) -&gt; BalancedAccuracyResult\n</code></pre> <p>Compute the balanced accuracy score.</p> <p>Parameters:</p> Name Type Description Default <code>y_true</code> <code>ArrayLike</code> <p>The ground truth target labels.</p> required <code>y_pred</code> <code>ArrayLike</code> <p>The predicted labels.</p> required <code>missing_policy</code> <code>MissingPolicy</code> <p>The policy for handling missing values. Valid values are <code>'omit'</code>, <code>'propagate'</code>, or <code>'raise'</code>. <code>'omit'</code> removes rows where any array equals <code>missing_values</code> before computing the metric. <code>'propagate'</code> keeps the rows but returns <code>balanced_accuracy=nan</code> when missing values are detected. <code>'raise'</code> raises a <code>ValueError</code> if any array contains <code>missing_values</code>.</p> <code>'propagate'</code> <code>missing_values</code> <code>Any</code> <p>The value to treat as missing. If not set, missing value handling is disabled regardless of <code>missing_policy</code>.</p> <code>NOT_SET</code> <code>raise_empty</code> <code>bool</code> <p>If <code>True</code>, raises <code>EmptyMetricError</code> when there are no valid predictions. If <code>False</code>, returns a <code>BalancedAccuracyResult</code> with <code>num_predictions=0</code>.</p> <code>True</code> <p>Returns:</p> Type Description <code>BalancedAccuracyResult</code> <p>The balanced accuracy result. When missing values are present and <code>missing_policy='propagate'</code>, the result keeps <code>num_predictions</code> and sets <code>balanced_accuracy</code> to <code>nan</code>.</p> <p>Raises:</p> Type Description <code>EmptyMetricError</code> <p>if there are no valid predictions and <code>raise_empty</code> is <code>True</code>.</p> <code>ValueError</code> <p>if <code>missing_policy</code> is invalid.</p> <code>ValueError</code> <p>if <code>y_true</code> or <code>y_pred</code> contains <code>missing_values</code> and <code>missing_policy</code> is <code>'raise'</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.functional import balanced_accuracy\n&gt;&gt;&gt; # with numpy arrays\n&gt;&gt;&gt; balanced_accuracy(\n...     y_true=np.array([1, 0, 0, 1, 1]),\n...     y_pred=np.array([1, 0, 0, 1, 1]),\n... )\nBalancedAccuracyResult(balanced_accuracy=1.0, num_predictions=5)\n&gt;&gt;&gt; # with lists\n&gt;&gt;&gt; balanced_accuracy(y_true=[1, 0, 0, 1, 1], y_pred=[1, 0, 1, 1, 1])\nBalancedAccuracyResult(balanced_accuracy=0.75, num_predictions=5)\n&gt;&gt;&gt; # with string labels\n&gt;&gt;&gt; balanced_accuracy(\n...     y_true=[\"cat\", \"dog\", \"cat\", \"dog\"],\n...     y_pred=[\"cat\", \"dog\", \"dog\", \"dog\"],\n... )\nBalancedAccuracyResult(balanced_accuracy=0.75, num_predictions=4)\n&gt;&gt;&gt; # with missing values and missing_policy='propagate' (default)\n&gt;&gt;&gt; balanced_accuracy(\n...     y_true=np.array([1.0, 0.0, 0.0, 1.0, float(\"nan\")]),\n...     y_pred=np.array([1.0, 0.0, 0.0, 1.0, 1.0]),\n...     missing_values=float(\"nan\"),\n... )\nBalancedAccuracyResult(balanced_accuracy=nan, num_predictions=5)\n&gt;&gt;&gt; # with missing values and missing_policy='omit'\n&gt;&gt;&gt; balanced_accuracy(\n...     y_true=np.array([1.0, 0.0, 0.0, 1.0, float(\"nan\")]),\n...     y_pred=np.array([1.0, 0.0, 0.0, 1.0, 1.0]),\n...     missing_policy=\"omit\",\n...     missing_values=float(\"nan\"),\n... )\nBalancedAccuracyResult(balanced_accuracy=1.0, num_predictions=4)\n&gt;&gt;&gt; # allow empty result instead of raising\n&gt;&gt;&gt; balanced_accuracy(y_true=[], y_pred=[], raise_empty=False)\nBalancedAccuracyResult(balanced_accuracy=nan, num_predictions=0)\n</code></pre>"},{"location":"refs/functional/#metriclab.functional.binary_precision","title":"metriclab.functional.binary_precision","text":"<pre><code>binary_precision(\n    y_true: ArrayLike,\n    y_pred: ArrayLike,\n    *,\n    pos_label: Any = 1,\n    missing_policy: MissingPolicy = \"propagate\",\n    missing_values: Any = NOT_SET,\n    raise_empty: bool = True\n) -&gt; BinaryPrecisionResult\n</code></pre> <p>Compute the binary precision score.</p> <p>Parameters:</p> Name Type Description Default <code>y_true</code> <code>ArrayLike</code> <p>The ground truth target labels.</p> required <code>y_pred</code> <code>ArrayLike</code> <p>The predicted labels.</p> required <code>pos_label</code> <code>Any</code> <p>The label of the positive class. Defaults to <code>1</code>.</p> <code>1</code> <code>missing_policy</code> <code>MissingPolicy</code> <p>The policy for handling missing values. Valid values are <code>'omit'</code>, <code>'propagate'</code>, or <code>'raise'</code>. <code>'omit'</code> removes rows where any array equals <code>missing_values</code> before computing the metric. <code>'propagate'</code> keeps the rows but returns <code>precision=nan</code> when missing values are detected. <code>'raise'</code> raises a <code>ValueError</code> if any array contains <code>missing_values</code>.</p> <code>'propagate'</code> <code>missing_values</code> <code>Any</code> <p>The value to treat as missing. If not set, missing value handling is disabled regardless of <code>missing_policy</code>.</p> <code>NOT_SET</code> <code>raise_empty</code> <code>bool</code> <p>If <code>True</code>, raises <code>EmptyMetricError</code> when there are no valid predictions. If <code>False</code>, returns a <code>BinaryPrecisionResult</code> with <code>num_predictions=0</code> and <code>precision=nan</code>.</p> <code>True</code> <p>Returns:</p> Type Description <code>BinaryPrecisionResult</code> <p>A <code>BinaryPrecisionResult</code>. When missing values are present and <code>missing_policy='propagate'</code>, the result keeps <code>num_predictions</code> and sets <code>precision</code> to <code>nan</code>.</p> <p>Raises:</p> Type Description <code>EmptyMetricError</code> <p>if there are no valid predictions and <code>raise_empty</code> is <code>True</code>.</p> <code>ValueError</code> <p>if <code>missing_policy</code> is invalid.</p> <code>ValueError</code> <p>if <code>y_true</code> or <code>y_pred</code> contains <code>missing_values</code> and <code>missing_policy</code> is <code>'raise'</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.functional import binary_precision\n&gt;&gt;&gt; # with numpy arrays\n&gt;&gt;&gt; binary_precision(\n...     y_true=np.array([1, 0, 0, 1, 1]),\n...     y_pred=np.array([1, 0, 0, 1, 1]),\n... )\nBinaryPrecisionResult(precision=1.0, num_predictions=5)\n&gt;&gt;&gt; # with lists\n&gt;&gt;&gt; binary_precision(y_true=[1, 0, 0, 1, 1], y_pred=[1, 0, 1, 1, 1])\nBinaryPrecisionResult(precision=0.75, num_predictions=5)\n&gt;&gt;&gt; # with string labels\n&gt;&gt;&gt; binary_precision(\n...     y_true=[\"cat\", \"dog\", \"cat\", \"dog\", \"dog\"],\n...     y_pred=[\"cat\", \"dog\", \"dog\", \"dog\", \"dog\"],\n...     pos_label=\"dog\",\n... )\nBinaryPrecisionResult(precision=0.75, num_predictions=5)\n&gt;&gt;&gt; # with missing values and missing_policy='propagate' (default)\n&gt;&gt;&gt; binary_precision(\n...     y_true=np.array([1.0, 0.0, 0.0, 1.0, float(\"nan\")]),\n...     y_pred=np.array([1.0, 0.0, 0.0, 1.0, 1.0]),\n...     missing_values=float(\"nan\"),\n... )\nBinaryPrecisionResult(precision=nan, num_predictions=5)\n&gt;&gt;&gt; # with missing values and missing_policy='omit'\n&gt;&gt;&gt; binary_precision(\n...     y_true=np.array([1.0, 0.0, 0.0, 1.0, float(\"nan\")]),\n...     y_pred=np.array([1.0, 0.0, 0.0, 1.0, 1.0]),\n...     missing_policy=\"omit\",\n...     missing_values=float(\"nan\"),\n... )\nBinaryPrecisionResult(precision=1.0, num_predictions=4)\n&gt;&gt;&gt; # allow empty result instead of raising\n&gt;&gt;&gt; binary_precision(y_true=[], y_pred=[], raise_empty=False)\nBinaryPrecisionResult(precision=nan, num_predictions=0)\n</code></pre>"},{"location":"refs/functional/#metriclab.functional.multiclass_precision","title":"metriclab.functional.multiclass_precision","text":"<pre><code>multiclass_precision(\n    y_true: ArrayLike,\n    y_pred: ArrayLike,\n    *,\n    missing_policy: MissingPolicy = \"propagate\",\n    missing_values: Any = NOT_SET,\n    raise_empty: bool = True,\n    undefined_policy: UndefinedPolicy = \"warn\"\n) -&gt; MulticlassPrecisionResult\n</code></pre> <p>Compute the multiclass precision score.</p> <p>Parameters:</p> Name Type Description Default <code>y_true</code> <code>ArrayLike</code> <p>The ground truth target labels.</p> required <code>y_pred</code> <code>ArrayLike</code> <p>The predicted labels.</p> required <code>missing_policy</code> <code>MissingPolicy</code> <p>The policy for handling missing values. Valid values are <code>'omit'</code>, <code>'propagate'</code>, or <code>'raise'</code>. <code>'omit'</code> removes rows where any array equals <code>missing_values</code> before computing the metric. <code>'propagate'</code> keeps the rows but returns <code>precision=nan</code> when missing values are detected. <code>'raise'</code> raises a <code>ValueError</code> if any array contains <code>missing_values</code>.</p> <code>'propagate'</code> <code>missing_values</code> <code>Any</code> <p>The value to treat as missing. If not set, missing value handling is disabled regardless of <code>missing_policy</code>.</p> <code>NOT_SET</code> <code>raise_empty</code> <code>bool</code> <p>If <code>True</code>, raises <code>EmptyMetricError</code> when there are no valid predictions. If <code>False</code>, returns a <code>MulticlassPrecisionResult</code> with <code>num_predictions=0</code> and <code>precision=nan</code>.</p> <code>True</code> <code>undefined_policy</code> <code>UndefinedPolicy</code> <p>The value to substitute when precision is undefined for a class because no samples were predicted as that class (i.e. <code>TP + FP == 0</code>). Accepted values:</p> <ul> <li><code>0.0</code>: substitute zero (matches sklearn default).</li> <li><code>1.0</code>: substitute one (optimistic; use when absent   classes should not penalise the score).</li> <li><code>float('nan')</code>: substitute NaN, which then propagates   into macro/weighted/micro averages.</li> <li><code>\"nan\"</code>: string alias for <code>float('nan')</code>.</li> <li><code>\"warn\"</code> (default): substitutes <code>0.0</code> and emits a   <code>UserWarning</code> listing the affected classes.</li> </ul> <code>'warn'</code> <p>Returns:</p> Type Description <code>MulticlassPrecisionResult</code> <p>A <code>MulticlassPrecisionResult</code>. When missing values are present and <code>missing_policy='propagate'</code>, the result keeps <code>num_predictions</code> and sets <code>precision</code> to <code>nan</code>.</p> <p>Raises:</p> Type Description <code>EmptyMetricError</code> <p>if there are no valid predictions and <code>raise_empty</code> is <code>True</code>.</p> <code>ValueError</code> <p>if <code>missing_policy</code> is invalid.</p> <code>ValueError</code> <p>if <code>y_true</code> or <code>y_pred</code> contains <code>missing_values</code> and <code>missing_policy</code> is <code>'raise'</code>.</p> <code>ValueError</code> <p>if <code>undefined_policy</code> is not one of the accepted values.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.functional import multiclass_precision\n&gt;&gt;&gt; # with numpy arrays\n&gt;&gt;&gt; multiclass_precision(\n...     y_true=np.array([1, 0, 0, 1, 1, 2]),\n...     y_pred=np.array([1, 0, 0, 1, 1, 2]),\n... )\nMulticlassPrecisionResult(macro_precision=1.0, micro_precision=1.0, weighted_precision=1.0, per_class_precision=array([1., 1., 1.]), support=array([2, 3, 1]), num_predictions=6)\n&gt;&gt;&gt; # with lists\n&gt;&gt;&gt; multiclass_precision(y_true=[1, 0, 0, 1, 1, 2], y_pred=[1, 0, 0, 1, 1, 2])\nMulticlassPrecisionResult(macro_precision=1.0, micro_precision=1.0, weighted_precision=1.0, per_class_precision=array([1., 1., 1.]), support=array([2, 3, 1]), num_predictions=6)\n&gt;&gt;&gt; # with string labels\n&gt;&gt;&gt; multiclass_precision(\n...     y_true=[\"cat\", \"dog\", \"cat\", \"dog\", \"dog\", \"bear\"],\n...     y_pred=[\"cat\", \"dog\", \"cat\", \"dog\", \"dog\", \"bear\"],\n... )\nMulticlassPrecisionResult(macro_precision=1.0, micro_precision=1.0, weighted_precision=1.0, per_class_precision=array([1., 1., 1.]), support=array([1, 2, 3]), num_predictions=6)\n&gt;&gt;&gt; # with missing values and missing_policy='propagate' (default)\n&gt;&gt;&gt; multiclass_precision(\n...     y_true=np.array([1.0, 0.0, 0.0, 1.0, float(\"nan\")]),\n...     y_pred=np.array([1.0, 0.0, 0.0, 1.0, 1.0]),\n...     missing_values=float(\"nan\"),\n... )\nMulticlassPrecisionResult(macro_precision=nan, micro_precision=nan, weighted_precision=nan, per_class_precision=array([nan, nan]), support=array([nan, nan]), num_predictions=5)\n&gt;&gt;&gt; # with missing values and missing_policy='omit'\n&gt;&gt;&gt; multiclass_precision(\n...     y_true=np.array([1.0, 0.0, 0.0, 1.0, float(\"nan\")]),\n...     y_pred=np.array([1.0, 0.0, 0.0, 1.0, 1.0]),\n...     missing_policy=\"omit\",\n...     missing_values=float(\"nan\"),\n... )\nMulticlassPrecisionResult(macro_precision=1.0, micro_precision=1.0, weighted_precision=1.0, per_class_precision=array([1., 1.]), support=array([2, 2]), num_predictions=4)\n&gt;&gt;&gt; # silence the undefined_policy warning explicitly\n&gt;&gt;&gt; multiclass_precision(\n...     y_true=[0, 0, 1, 2],\n...     y_pred=[0, 0, 1, 1],\n...     undefined_policy=0.0,\n... )\nMulticlassPrecisionResult(macro_precision=0.5, micro_precision=0.75, weighted_precision=0.625, per_class_precision=array([1. , 0.5, 0. ]), support=array([2, 1, 1]), num_predictions=4)\n&gt;&gt;&gt; # propagate NaN for undefined classes\n&gt;&gt;&gt; multiclass_precision(\n...     y_true=[0, 0, 1, 2],\n...     y_pred=[0, 0, 1, 1],\n...     undefined_policy=float(\"nan\"),\n... )\nMulticlassPrecisionResult(macro_precision=nan, micro_precision=0.75, weighted_precision=nan, per_class_precision=array([1. , 0.5, nan]), support=array([2, 1, 1]), num_predictions=4)\n</code></pre>"},{"location":"refs/metrics/","title":"Metrics","text":""},{"location":"refs/metrics/#metriclab.metrics","title":"metriclab.metrics","text":"<p>Namespace for stateful metric objects.</p> <p>This package is reserved for metric implementations that accumulate state across multiple updates before producing a final result.</p>"},{"location":"refs/results/","title":"Results","text":""},{"location":"refs/results/#metriclab.results","title":"metriclab.results","text":"<p>Immutable containers that store computed metric values.</p> <p>Result objects expose a consistent API to compare metric values, export aggregates as dictionaries, and format them for display.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import Result\n&gt;&gt;&gt; result = Result({\"accuracy\": 0.9, \"loss\": 0.1})\n&gt;&gt;&gt; result.to_dict(prefix=\"val_\")\n{'val_accuracy': 0.9, 'val_loss': 0.1}\n</code></pre>"},{"location":"refs/results/#metriclab.results.AccuracyResult","title":"metriclab.results.AccuracyResult  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Store aggregated values used to compute classification accuracy.</p> <p>The inherited <code>to_dict</code> method exports both the derived <code>accuracy</code> value and the raw counts used to compute it. <code>to_display</code> returns a compact textual summary for terminal output, and <code>combine</code> merges two partial aggregations.</p> <p>Attributes:</p> Name Type Description <code>num_correct_predictions</code> <code>int | float</code> <p>The number of correct predictions. Set to NaN if the number of correct predictions is unknown because of missing values.</p> <code>num_predictions</code> <code>int</code> <p>The number of predictions.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>num_predictions</code> is negative, if <code>num_correct_predictions</code> is negative, or if <code>num_correct_predictions</code> is greater than <code>num_predictions</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import AccuracyResult\n&gt;&gt;&gt; m = AccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m\nAccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m.to_dict()\n{'accuracy': 0.7, 'num_correct_predictions': 7, 'num_predictions': 10}\n&gt;&gt;&gt; print(m.to_display())\nAccuracy [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591]  0.7000  (7/10)\n</code></pre>"},{"location":"refs/results/#metriclab.results.AccuracyResult.accuracy","title":"metriclab.results.AccuracyResult.accuracy  <code>property</code>","text":"<pre><code>accuracy: float\n</code></pre> <p>Return the accuracy ratio.</p> <p>Returns:</p> Type Description <code>float</code> <p>The ratio <code>num_correct_predictions / num_predictions</code>.</p> <code>float</code> <p>Returns <code>nan</code> when <code>num_predictions</code> is <code>0</code>.</p>"},{"location":"refs/results/#metriclab.results.AccuracyResult.combine","title":"metriclab.results.AccuracyResult.combine","text":"<pre><code>combine(other: AccuracyResult) -&gt; AccuracyResult\n</code></pre> <p>Merge two partial accuracy results.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>AccuracyResult</code> <p>Another accuracy result to combine with <code>self</code>.</p> required <p>Returns:</p> Type Description <code>AccuracyResult</code> <p>A new accuracy result whose counts are the sum of both results.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>other</code> is not an :class:<code>AccuracyResult</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import AccuracyResult\n&gt;&gt;&gt; AccuracyResult(3, 4).combine(AccuracyResult(5, 6))\nAccuracyResult(num_correct_predictions=8, num_predictions=10)\n</code></pre>"},{"location":"refs/results/#metriclab.results.BalancedAccuracyResult","title":"metriclab.results.BalancedAccuracyResult  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Store aggregated values for balanced classification accuracy.</p> <p>Balanced accuracy is the macro-average of per-class recall. Unlike standard accuracy, it is robust to class imbalance.</p> <p>Attributes:</p> Name Type Description <code>balanced_accuracy</code> <code>float</code> <p>The balanced accuracy score, in <code>[0, 1]</code>, or <code>nan</code> if it could not be computed.</p> <code>num_predictions</code> <code>int</code> <p>The total number of predictions.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>num_predictions</code> is negative.</p> <code>ValueError</code> <p>if <code>balanced_accuracy</code> is negative and not <code>nan</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import BalancedAccuracyResult\n&gt;&gt;&gt; m = BalancedAccuracyResult(balanced_accuracy=0.7, num_predictions=10)\n&gt;&gt;&gt; m\nBalancedAccuracyResult(balanced_accuracy=0.7, num_predictions=10)\n&gt;&gt;&gt; m.to_dict()\n{'balanced_accuracy': 0.7, 'num_predictions': 10}\n&gt;&gt;&gt; print(m.to_display())\nBalanced Accuracy [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591]  0.7000\n</code></pre>"},{"location":"refs/results/#metriclab.results.BaseResult","title":"metriclab.results.BaseResult","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for immutable metric result containers.</p> <p>Subclasses implement a common interface for equality checks, serialization, and display formatting.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import AccuracyResult\n&gt;&gt;&gt; m = AccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m\nAccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m.to_dict()\n{'accuracy': 0.7, 'num_correct_predictions': 7, 'num_predictions': 10}\n</code></pre>"},{"location":"refs/results/#metriclab.results.BaseResult.allclose","title":"metriclab.results.BaseResult.allclose  <code>abstractmethod</code>","text":"<pre><code>allclose(\n    other: object,\n    *,\n    rtol: float = 1e-05,\n    atol: float = 1e-08,\n    equal_nan: bool = False\n) -&gt; bool\n</code></pre> <p>Check whether two results are numerically close.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>The object to be compared with.</p> required <code>rtol</code> <code>float</code> <p>The relative tolerance parameter. Must be non-negative.</p> <code>1e-05</code> <code>atol</code> <code>float</code> <p>The absolute tolerance parameter. Must be non-negative.</p> <code>1e-08</code> <code>equal_nan</code> <code>bool</code> <p>If <code>True</code>, then two <code>NaN</code>s  will be considered as equal.</p> <code>False</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the two objects are (element-wise) equal within a tolerance, otherwise <code>False</code></p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import AccuracyResult\n&gt;&gt;&gt; m1 = AccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m2 = AccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m3 = AccuracyResult(num_correct_predictions=5, num_predictions=10)\n&gt;&gt;&gt; m1.allclose(m2)\nTrue\n&gt;&gt;&gt; m1.allclose(m3)\nFalse\n</code></pre>"},{"location":"refs/results/#metriclab.results.BaseResult.equal","title":"metriclab.results.BaseResult.equal  <code>abstractmethod</code>","text":"<pre><code>equal(other: object, equal_nan: bool = False) -&gt; bool\n</code></pre> <p>Check whether two results are exactly equal.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>The value to compare with.</p> required <code>equal_nan</code> <code>bool</code> <p>Whether to compare NaN's as equal. If <code>True</code>, NaN's in both objects will be considered equal.</p> <code>False</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the two objects are equal, otherwise <code>False</code></p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import AccuracyResult\n&gt;&gt;&gt; m1 = AccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m2 = AccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m3 = AccuracyResult(num_correct_predictions=5, num_predictions=10)\n&gt;&gt;&gt; m1.equal(m2)\nTrue\n&gt;&gt;&gt; m1.equal(m3)\nFalse\n</code></pre>"},{"location":"refs/results/#metriclab.results.BaseResult.to_dict","title":"metriclab.results.BaseResult.to_dict  <code>abstractmethod</code>","text":"<pre><code>to_dict(\n    prefix: str = \"\", suffix: str = \"\"\n) -&gt; dict[str, Any]\n</code></pre> <p>Export the result as a dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>The prefix to add to each key of the result.</p> <code>''</code> <code>suffix</code> <code>str</code> <p>The suffix to add to each key of the result.</p> <code>''</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>The dictionary representation of the result.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import AccuracyResult\n&gt;&gt;&gt; m = AccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; m.to_dict(prefix=\"val_\", suffix=\"_score\")\n{'val_accuracy_score': 0.7, 'val_num_correct_predictions_score': 7, 'val_num_predictions_score': 10}\n</code></pre>"},{"location":"refs/results/#metriclab.results.BaseResult.to_display","title":"metriclab.results.BaseResult.to_display  <code>abstractmethod</code>","text":"<pre><code>to_display() -&gt; str\n</code></pre> <p>Return a human-readable summary string.</p> <p>Returns:</p> Type Description <code>str</code> <p>A human-readable summary string</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import AccuracyResult\n&gt;&gt;&gt; m = AccuracyResult(num_correct_predictions=7, num_predictions=10)\n&gt;&gt;&gt; print(m.to_display())\nAccuracy [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591]  0.7000  (7/10)\n</code></pre>"},{"location":"refs/results/#metriclab.results.BinaryConfusionMatrixResult","title":"metriclab.results.BinaryConfusionMatrixResult  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Store aggregated values from a binary confusion matrix used to compute classification metrics including accuracy, precision, recall, specificity, and F-beta scores.</p> <p>Use :meth:<code>from_confusion_matrix</code> to construct an instance from raw confusion matrix counts. Any count set to <code>nan</code> propagates to all derived metrics.</p> <p>Attributes:</p> Name Type Description <code>true_positives</code> <code>int | float</code> <p>The number of true positives, or <code>nan</code>.</p> <code>true_negatives</code> <code>int | float</code> <p>The number of true negatives, or <code>nan</code>.</p> <code>false_positives</code> <code>int | float</code> <p>The number of false positives, or <code>nan</code>.</p> <code>false_negatives</code> <code>int | float</code> <p>The number of false negatives, or <code>nan</code>.</p> <code>num_predictions</code> <code>int | float</code> <p>The total number of predictions, or <code>nan</code>.</p> <code>num_correct_predictions</code> <code>int | float</code> <p>The number of correct predictions, or <code>nan</code>.</p> <code>accuracy</code> <code>float</code> <p>The accuracy score, or <code>nan</code>.</p> <code>precision</code> <code>float</code> <p>The precision score, or <code>nan</code>.</p> <code>recall</code> <code>float</code> <p>The recall score, or <code>nan</code>.</p> <code>specificity</code> <code>float</code> <p>The specificity score, or <code>nan</code>.</p> <code>f_beta_scores</code> <code>dict[float, float]</code> <p>A mapping of beta values to F-beta scores.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import BinaryConfusionMatrixResult\n&gt;&gt;&gt; m = BinaryConfusionMatrixResult.from_confusion_matrix(\n...     true_positives=3,\n...     true_negatives=4,\n...     false_positives=1,\n...     false_negatives=2,\n... )\n&gt;&gt;&gt; m\nBinaryConfusionMatrixResult(true_positives=3, true_negatives=4, false_positives=1, false_negatives=2, num_predictions=10, num_correct_predictions=7, accuracy=0.7, precision=0.75, recall=0.6, specificity=0.8, f_beta_scores={1.0: 0.6666...})\n&gt;&gt;&gt; m.accuracy\n0.7\n&gt;&gt;&gt; m.precision\n0.75\n&gt;&gt;&gt; m.recall\n0.6\n&gt;&gt;&gt; m.specificity\n0.8\n&gt;&gt;&gt; m.f_beta_scores\n{1.0: 0.6666666666666665}\n&gt;&gt;&gt; print(m.to_display())\nBinary Confusion Matrix\n-----------------------\nn=10  TP=3  TN=4  FP=1  FN=2\nAccuracy    [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591]  0.7000  (7/10)\nPrecision   [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591]  0.7500  (3/4)\nRecall      [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591\u2591\u2591]  0.6000  (3/5)\nSpecificity [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591]  0.8000  (4/5)\nF1          [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591\u2591]  0.6667\n\n&gt;&gt;&gt; # NaN propagates to derived metrics\n&gt;&gt;&gt; m_nan = BinaryConfusionMatrixResult.from_confusion_matrix(\n...     true_positives=float(\"nan\"),\n...     true_negatives=4,\n...     false_positives=1,\n...     false_negatives=2,\n... )\n&gt;&gt;&gt; m_nan.accuracy\nnan\n</code></pre>"},{"location":"refs/results/#metriclab.results.BinaryConfusionMatrixResult.combine","title":"metriclab.results.BinaryConfusionMatrixResult.combine","text":"<pre><code>combine(\n    other: BinaryConfusionMatrixResult,\n) -&gt; BinaryConfusionMatrixResult\n</code></pre> <p>Merge two partial binary confusion matrix results.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>BinaryConfusionMatrixResult</code> <p>Another binary confusion matrix result to combine with <code>self</code>.</p> required <p>Returns:</p> Type Description <code>BinaryConfusionMatrixResult</code> <p>A new result whose confusion matrix counts are the element-wise sum of both results.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>other</code> is not a :class:<code>BinaryConfusionMatrixResult</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import BinaryConfusionMatrixResult\n&gt;&gt;&gt; r1 = BinaryConfusionMatrixResult.from_confusion_matrix(\n...     true_positives=3,\n...     true_negatives=4,\n...     false_positives=1,\n...     false_negatives=2,\n... )\n&gt;&gt;&gt; r2 = BinaryConfusionMatrixResult.from_confusion_matrix(\n...     true_positives=2,\n...     true_negatives=3,\n...     false_positives=0,\n...     false_negatives=1,\n... )\n&gt;&gt;&gt; r1.combine(r2).num_predictions\n16\n</code></pre>"},{"location":"refs/results/#metriclab.results.BinaryConfusionMatrixResult.from_confusion_matrix","title":"metriclab.results.BinaryConfusionMatrixResult.from_confusion_matrix  <code>classmethod</code>","text":"<pre><code>from_confusion_matrix(\n    true_positives: float,\n    true_negatives: float,\n    false_positives: float,\n    false_negatives: float,\n    betas: Sequence[float] = (1.0,),\n) -&gt; BinaryConfusionMatrixResult\n</code></pre> <p>Create a result from raw confusion matrix counts.</p> <p>Any count set to <code>nan</code> propagates to all derived metrics (<code>num_predictions</code>, <code>num_correct_predictions</code>, <code>accuracy</code>, <code>precision</code>, <code>recall</code>, <code>specificity</code>, and all F-beta scores).</p> <p>Parameters:</p> Name Type Description Default <code>true_positives</code> <code>float</code> <p>The number of true positives, or <code>nan</code>.</p> required <code>true_negatives</code> <code>float</code> <p>The number of true negatives, or <code>nan</code>.</p> required <code>false_positives</code> <code>float</code> <p>The number of false positives, or <code>nan</code>.</p> required <code>false_negatives</code> <code>float</code> <p>The number of false negatives, or <code>nan</code>.</p> required <code>betas</code> <code>Sequence[float]</code> <p>The beta values for F-beta score computation. Defaults to <code>(1.0,)</code> which gives the F1 score.</p> <code>(1.0,)</code> <p>Returns:</p> Type Description <code>BinaryConfusionMatrixResult</code> <p>A fully populated <code>BinaryConfusionMatrixResult</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if any non-NaN count is negative.</p> <code>ValueError</code> <p>if any beta value is negative.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import BinaryConfusionMatrixResult\n&gt;&gt;&gt; m = BinaryConfusionMatrixResult.from_confusion_matrix(\n...     true_positives=3,\n...     true_negatives=4,\n...     false_positives=1,\n...     false_negatives=2,\n...     betas=[0.5, 1.0, 2.0],\n... )\n&gt;&gt;&gt; m.f_beta_scores\n{0.5: 0.7142857142857143, 1.0: 0.6666666666666665, 2.0: 0.625}\n&gt;&gt;&gt; import math\n&gt;&gt;&gt; m_nan = BinaryConfusionMatrixResult.from_confusion_matrix(\n...     true_positives=float(\"nan\"),\n...     true_negatives=4,\n...     false_positives=1,\n...     false_negatives=2,\n... )\n&gt;&gt;&gt; math.isnan(m_nan.precision)\nTrue\n</code></pre>"},{"location":"refs/results/#metriclab.results.BinaryPrecisionResult","title":"metriclab.results.BinaryPrecisionResult  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Store aggregated values for binary classification precision.</p> <p>Precision is defined as <code>TP / (TP + FP)</code>, i.e. the fraction of positive predictions that are correct. It measures how many of the predicted positives are actually positive.</p> <p>Attributes:</p> Name Type Description <code>precision</code> <code>float</code> <p>The precision score, in <code>[0, 1]</code>, or <code>nan</code> if it could not be computed.</p> <code>num_predictions</code> <code>int</code> <p>The total number of predictions.</p> <code>num_positive_predictions</code> <code>int | float</code> <p>The number of predictions where the predicted label is the positive class (i.e. <code>TP + FP</code>).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>num_predictions</code> is negative.</p> <code>ValueError</code> <p>if <code>num_positive_predictions</code> is negative.</p> <code>ValueError</code> <p>if <code>num_positive_predictions</code> is greater than <code>num_predictions</code>.</p> <code>ValueError</code> <p>if <code>precision</code> is negative and not <code>nan</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import BinaryPrecisionResult\n&gt;&gt;&gt; m = BinaryPrecisionResult(\n...     precision=0.75, num_predictions=10, num_positive_predictions=4\n... )\n&gt;&gt;&gt; m\nBinaryPrecisionResult(precision=0.75, num_predictions=10, num_positive_predictions=4)\n&gt;&gt;&gt; m.precision\n0.75\n&gt;&gt;&gt; m.to_dict()\n{'precision': 0.75, 'num_predictions': 10, 'num_positive_predictions': 4}\n&gt;&gt;&gt; print(m.to_display())\nPrecision [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591]  0.7500  (3/4)  [n=10]\n</code></pre>"},{"location":"refs/results/#metriclab.results.MulticlassPrecisionResult","title":"metriclab.results.MulticlassPrecisionResult  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Store aggregated values for multiclass classification precision.</p> <p>Stores per-class precision scores alongside macro, micro, and weighted averages, consistent with :func:<code>sklearn.metrics.precision_score</code>.</p> <p>Attributes:</p> Name Type Description <code>macro_precision</code> <code>float</code> <p>Unweighted mean of per-class precision scores.</p> <code>micro_precision</code> <code>float</code> <p>Global precision computed from total true/false positives across all classes.</p> <code>weighted_precision</code> <code>float</code> <p>Per-class precision weighted by class support (number of actual instances per class).</p> <code>per_class_precision</code> <code>ndarray</code> <p>A 1D array of per-class precision scores, one entry per class.</p> <code>support</code> <code>ndarray</code> <p>A 1D array of per-class support counts (number of actual instances per class), same length as <code>per_class_precision</code>.</p> <code>num_predictions</code> <code>int</code> <p>The total number of predictions.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>num_predictions</code> is negative.</p> <code>ValueError</code> <p>if <code>macro_precision</code> is negative and not <code>nan</code>.</p> <code>ValueError</code> <p>if <code>micro_precision</code> is negative and not <code>nan</code>.</p> <code>ValueError</code> <p>if <code>weighted_precision</code> is negative and not <code>nan</code>.</p> <code>ValueError</code> <p>if <code>per_class_precision</code> and <code>support</code> have different shapes.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.results import MulticlassPrecisionResult\n&gt;&gt;&gt; m = MulticlassPrecisionResult(\n...     macro_precision=0.7,\n...     micro_precision=0.72,\n...     weighted_precision=0.71,\n...     per_class_precision=np.array([0.8, 0.6, 0.7]),\n...     support=np.array([100, 50, 150]),\n...     num_predictions=300,\n... )\n&gt;&gt;&gt; m.macro_precision\n0.7\n&gt;&gt;&gt; m.to_dict()  # doctest: +NORMALIZE_WHITESPACE\n{'macro_precision': 0.7, 'micro_precision': 0.72,\n 'weighted_precision': 0.71, 'per_class_precision': [0.8, 0.6, 0.7],\n 'support': [100, 50, 150], 'num_predictions': 300}\n&gt;&gt;&gt; print(m.to_display())\nPrecision  [n=300]\n----------------------------------------------------\nMacro       [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591]  0.7000\nMicro       [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591]  0.7200\nWeighted    [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591]  0.7100\nPer class:\n  class 0   [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591]  0.8000  [n=100]\n  class 1   [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591\u2591\u2591]  0.6000  [n=50]\n  class 2   [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591]  0.7000  [n=150]\n</code></pre>"},{"location":"refs/results/#metriclab.results.PrecisionResult","title":"metriclab.results.PrecisionResult  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Store aggregated values for classification precision.</p> <p>Precision is defined as <code>TP / (TP + FP)</code>, i.e. the fraction of positive predictions that are correct. It measures how many of the predicted positives are actually positive.</p> <p>Attributes:</p> Name Type Description <code>true_positives</code> <code>int | float</code> <p>The number of true positives (<code>TP</code>).</p> <code>false_positives</code> <code>int | float</code> <p>The number of false positives (<code>FP</code>).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>true_positives</code> is negative and not <code>nan</code>.</p> <code>ValueError</code> <p>if <code>false_positives</code> is negative and not <code>nan</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import PrecisionResult\n&gt;&gt;&gt; m = PrecisionResult(true_positives=3, false_positives=1)\n&gt;&gt;&gt; m\nPrecisionResult(true_positives=3, false_positives=1)\n&gt;&gt;&gt; m.precision\n0.75\n&gt;&gt;&gt; m.num_positive_predictions\n4\n&gt;&gt;&gt; m.to_dict()\n{'precision': 0.75, 'true_positives': 3, 'false_positives': 1, 'num_positive_predictions': 4}\n&gt;&gt;&gt; print(m.to_display())\nPrecision [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591]  0.7500  (3/4)\n</code></pre>"},{"location":"refs/results/#metriclab.results.PrecisionResult.num_positive_predictions","title":"metriclab.results.PrecisionResult.num_positive_predictions  <code>property</code>","text":"<pre><code>num_positive_predictions: int | float\n</code></pre> <p>Return the number of positive predictions (<code>TP + FP</code>).</p> <p>Returns:</p> Type Description <code>int | float</code> <p>The sum <code>true_positives + false_positives</code>, or <code>nan</code> if either count is <code>nan</code>.</p>"},{"location":"refs/results/#metriclab.results.PrecisionResult.precision","title":"metriclab.results.PrecisionResult.precision  <code>property</code>","text":"<pre><code>precision: float\n</code></pre> <p>Return the precision score.</p> <p>Returns:</p> Type Description <code>float</code> <p><code>true_positives / (true_positives + false_positives)</code>. Returns <code>nan</code> when either count is <code>nan</code>. Returns <code>0.0</code> when <code>true_positives + false_positives</code> is <code>0</code>.</p>"},{"location":"refs/results/#metriclab.results.PrecisionResult.from_precision","title":"metriclab.results.PrecisionResult.from_precision  <code>classmethod</code>","text":"<pre><code>from_precision(\n    precision: float, num_positive_predictions: float\n) -&gt; Self\n</code></pre> <p>Create a <code>PrecisionResult</code> from a precision score and the number of positive predictions.</p> <p>The number of true positives is back-computed as <code>round(precision * num_positive_predictions)</code> and false positives as the remainder.</p> <p>Parameters:</p> Name Type Description Default <code>precision</code> <code>float</code> <p>The precision score, in <code>[0, 1]</code>, or <code>nan</code>.</p> required <code>num_positive_predictions</code> <code>float</code> <p>The number of predicted positives (<code>TP + FP</code>).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A <code>PrecisionResult</code> with computed <code>true_positives</code> and <code>false_positives</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import PrecisionResult\n&gt;&gt;&gt; m = PrecisionResult.from_precision(precision=0.75, num_positive_predictions=4)\n&gt;&gt;&gt; m.true_positives\n3\n&gt;&gt;&gt; m.false_positives\n1\n&gt;&gt;&gt; m.precision\n0.75\n</code></pre>"},{"location":"refs/results/#metriclab.results.RecallResult","title":"metriclab.results.RecallResult  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Store aggregated values for classification recall.</p> <p>Recall is defined as <code>TP / (TP + FN)</code>, i.e. the fraction of actual positives that are correctly identified. It measures how many of the true positives are captured by the model.</p> <p>Attributes:</p> Name Type Description <code>true_positives</code> <code>int | float</code> <p>The number of true positives (<code>TP</code>).</p> <code>false_negatives</code> <code>int | float</code> <p>The number of false negatives (<code>FN</code>).</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>true_positives</code> is negative and not <code>nan</code>.</p> <code>ValueError</code> <p>if <code>false_negatives</code> is negative and not <code>nan</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import RecallResult\n&gt;&gt;&gt; m = RecallResult(true_positives=3, false_negatives=2)\n&gt;&gt;&gt; m\nRecallResult(true_positives=3, false_negatives=2)\n&gt;&gt;&gt; m.recall\n0.6\n&gt;&gt;&gt; m.num_actual_positives\n5\n&gt;&gt;&gt; m.to_dict()\n{'recall': 0.6, 'true_positives': 3, 'false_negatives': 2, 'num_actual_positives': 5}\n&gt;&gt;&gt; print(m.to_display())\nRecall [\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2591\u2591\u2591\u2591\u2591\u2591\u2591\u2591]  0.6000  (3/5)\n</code></pre>"},{"location":"refs/results/#metriclab.results.RecallResult.num_actual_positives","title":"metriclab.results.RecallResult.num_actual_positives  <code>property</code>","text":"<pre><code>num_actual_positives: int | float\n</code></pre> <p>Return the number of actual positives (<code>TP + FN</code>).</p> <p>Returns:</p> Type Description <code>int | float</code> <p>The sum <code>true_positives + false_negatives</code>, or <code>nan</code></p> <code>int | float</code> <p>if either count is <code>nan</code>.</p>"},{"location":"refs/results/#metriclab.results.RecallResult.recall","title":"metriclab.results.RecallResult.recall  <code>property</code>","text":"<pre><code>recall: float\n</code></pre> <p>Return the recall score.</p> <p>Returns:</p> Type Description <code>float</code> <p><code>true_positives / (true_positives + false_negatives)</code>.</p> <code>float</code> <p>Returns <code>nan</code> when either count is <code>nan</code>. Returns</p> <code>float</code> <p><code>0.0</code> when <code>true_positives + false_negatives</code> is <code>0</code>.</p>"},{"location":"refs/results/#metriclab.results.RecallResult.from_recall","title":"metriclab.results.RecallResult.from_recall  <code>classmethod</code>","text":"<pre><code>from_recall(\n    recall: float, num_actual_positives: float\n) -&gt; Self\n</code></pre> <p>Create a <code>RecallResult</code> from a recall score and the number of actual positives.</p> <p>The number of true positives is back-computed as <code>round(recall * num_actual_positives)</code> and false negatives as the remainder.</p> <p>Parameters:</p> Name Type Description Default <code>recall</code> <code>float</code> <p>The recall score, in <code>[0, 1]</code>, or <code>nan</code>.</p> required <code>num_actual_positives</code> <code>float</code> <p>The number of actual positives (<code>TP + FN</code>).</p> required <p>Returns:</p> Type Description <code>Self</code> <p>A <code>RecallResult</code> with computed <code>true_positives</code> and <code>false_negatives</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import RecallResult\n&gt;&gt;&gt; m = RecallResult.from_recall(recall=0.6, num_actual_positives=5)\n&gt;&gt;&gt; m.true_positives\n3\n&gt;&gt;&gt; m.false_negatives\n2\n&gt;&gt;&gt; m.recall\n0.6\n</code></pre>"},{"location":"refs/results/#metriclab.results.Result","title":"metriclab.results.Result  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Implement a simple metric result container.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>dict[str, Any]</code> <p>A dictionary containing metric names and values.</p> required <p><code>to_dict</code> returns the same key-value pairs as <code>results</code> when no prefix or suffix is provided; keys are prefixed and suffixed otherwise. <code>to_display</code> renders the exported dictionary with :class:<code>str</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import Result\n&gt;&gt;&gt; result = Result({\"accuracy\": 0.8, \"loss\": 0.2})\n&gt;&gt;&gt; result.to_dict(suffix=\"_metric\")\n{'accuracy_metric': 0.8, 'loss_metric': 0.2}\n&gt;&gt;&gt; print(result.to_display())\n{'accuracy': 0.8, 'loss': 0.2}\n</code></pre>"},{"location":"refs/results/#metriclab.results.ResultDict","title":"metriclab.results.ResultDict  <code>dataclass</code>","text":"<p>               Bases: <code>BaseResult</code></p> <p>Implement a result that is a container for a dict of results.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>dict[str, BaseResult]</code> <p>A mapping from string keys to :class:<code>BaseResult</code> instances. Each key typically identifies a dataset split or evaluation phase (e.g. <code>\"train\"</code>, <code>\"val\"</code>).</p> required <p><code>to_display</code> renders each result under a <code>=== key ===</code> heading, with a blank line between entries. An empty mapping produces an empty string.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.results import Result, ResultDict\n&gt;&gt;&gt; result = ResultDict({\"train\": Result({\"loss\": 0.5}), \"val\": Result({\"loss\": 0.3})})\n&gt;&gt;&gt; result.to_dict()\n{'train': {'loss': 0.5}, 'val': {'loss': 0.3}}\n&gt;&gt;&gt; print(result.to_display())\n=== train ===\n{'loss': 0.5}\n&lt;BLANKLINE&gt;\n=== val ===\n{'loss': 0.3}\n&lt;BLANKLINE&gt;\n</code></pre>"},{"location":"refs/typing/","title":"Typing","text":""},{"location":"refs/typing/#metriclab.typing","title":"metriclab.typing","text":"<p>Shared typing aliases used by the public API.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.typing import ArrayLike\n&gt;&gt;&gt; values: ArrayLike = [1, 0, 1]\n&gt;&gt;&gt; values\n[1, 0, 1]\n</code></pre>"},{"location":"refs/utils/","title":"Utils","text":""},{"location":"refs/utils/#metriclab.utils","title":"metriclab.utils","text":"<p>Supporting utilities used across metriclab.</p> <p>This package groups helpers for array validation, logging, formatting, missing value policies, sentinel objects, and optional dependencies.</p>"},{"location":"refs/utils/#metriclab.utils.array","title":"metriclab.utils.array","text":"<p>Helpers to validate and process array-like inputs.</p>"},{"location":"refs/utils/#metriclab.utils.array.check_nan_policy","title":"metriclab.utils.array.check_nan_policy","text":"<pre><code>check_nan_policy(\n    arr: ndarray,\n    nan_policy: NanPolicy = \"propagate\",\n    name: str = \"input\",\n) -&gt; bool\n</code></pre> <p>Indicate if the given array contains at least one NaN value.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>The array to check.</p> required <code>nan_policy</code> <code>NanPolicy</code> <p>The NaN policy. The valid values are <code>'omit'</code>, <code>'propagate'</code>, or <code>'raise'</code>.</p> <code>'propagate'</code> <code>name</code> <code>str</code> <p>An optional name to be more precise about the array when the exception is raised.</p> <code>'input'</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the array contains at least one NaN value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if the array contains at least one NaN value and <code>nan_policy</code> is <code>'raise'</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import check_nan_policy\n&gt;&gt;&gt; check_nan_policy(np.array([1, 2, 3]))\nFalse\n&gt;&gt;&gt; check_nan_policy(np.array([1, 2, np.nan]))\nTrue\n&gt;&gt;&gt; check_nan_policy(np.array([1, 2, float(\"nan\")], dtype=object))\nTrue\n&gt;&gt;&gt; check_nan_policy(np.array([\"a\", \"b\", \"c\"]))\nFalse\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.contains_nan","title":"metriclab.utils.array.contains_nan","text":"<pre><code>contains_nan(arr: ndarray) -&gt; bool\n</code></pre> <p>Indicate if the given array contains at least one NaN value.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>The array to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the array contains at least one NaN value.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import contains_nan\n&gt;&gt;&gt; contains_nan(np.array([1, 2, 3]))\nFalse\n&gt;&gt;&gt; contains_nan(np.array([1, 2, np.nan]))\nTrue\n&gt;&gt;&gt; contains_nan(np.array([1, 2, float(\"nan\")], dtype=object))\nTrue\n&gt;&gt;&gt; contains_nan(np.array([\"a\", \"b\", \"c\"]))\nFalse\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.contains_value","title":"metriclab.utils.array.contains_value","text":"<pre><code>contains_value(arr: ArrayLike, value: Any) -&gt; bool\n</code></pre> <p>Check if a value is present in a numpy array.</p> <p>Works correctly with special values: <code>nan</code> (using :func:<code>numpy.isnan</code> since <code>nan != nan</code>), <code>inf</code> (using :func:<code>numpy.isinf</code> to distinguish <code>+inf</code> from <code>-inf</code>), and <code>None</code> (using identity check in object arrays).</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ArrayLike</code> <p>The array to search.</p> required <code>value</code> <code>Any</code> <p>The value to search for.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>value</code> is present in <code>arr</code>, <code>False</code> otherwise.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import contains_value\n&gt;&gt;&gt; contains_value(np.array([1, 2, 3]), 2)\nTrue\n&gt;&gt;&gt; contains_value(np.array([1, 2, 3]), 4)\nFalse\n&gt;&gt;&gt; contains_value(np.array([1.0, float(\"nan\"), 3.0]), float(\"nan\"))\nTrue\n&gt;&gt;&gt; contains_value(np.array([1.0, float(\"inf\"), 3.0]), float(\"inf\"))\nTrue\n&gt;&gt;&gt; contains_value(np.array([1.0, float(\"-inf\"), 3.0]), float(\"-inf\"))\nTrue\n&gt;&gt;&gt; contains_value(np.array([1, None, 3], dtype=object), None)\nTrue\n&gt;&gt;&gt; contains_value(np.array([1, 2, 3]), None)\nFalse\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.count_unique_non_missing","title":"metriclab.utils.array.count_unique_non_missing","text":"<pre><code>count_unique_non_missing(\n    arr: ArrayLike, missing_values: Any = NOT_SET\n) -&gt; int\n</code></pre> <p>Count the number of unique non-missing values.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ArrayLike</code> <p>Input array-like object.</p> required <code>missing_values</code> <code>Any</code> <p>Value representing missing data. If <code>NOT_SET</code>, all unique values are counted.</p> <code>NOT_SET</code> <p>Returns:</p> Type Description <code>int</code> <p>The number of unique non-missing values.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import count_unique_non_missing\n&gt;&gt;&gt; count_unique_non_missing(np.array([1, 0, 0, 1, 1, 2]))\n3\n&gt;&gt;&gt; count_unique_non_missing(np.array([1, 0, 0, 1, 1, 2, float(\"nan\")]))\n4\n&gt;&gt;&gt; count_unique_non_missing(\n...     np.array([1, 0, 0, 1, 1, 2, float(\"nan\")]),\n...     missing_values=float(\"nan\"),\n... )\n3\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.count_values","title":"metriclab.utils.array.count_values","text":"<pre><code>count_values(arr: ndarray, value: Any) -&gt; int\n</code></pre> <p>Count the number of occurrences of value in arr.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>An array to count values in. Arrays of any shape are accepted.</p> required <code>value</code> <code>Any</code> <p>The value to count. If value is NaN, all NaN values are counted regardless of sign or dtype.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The number of occurrences of value in arr.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; count_values(np.array([1, 2, 1, 3, 1]), 1)\n3\n&gt;&gt;&gt; count_values(np.array([1.0, float(\"nan\"), 2.0, float(\"nan\")]), float(\"nan\"))\n2\n&gt;&gt;&gt; count_values(np.array([\"cat\", \"dog\", \"cat\"], dtype=object), \"cat\")\n2\n&gt;&gt;&gt; count_values(np.array([[1, 2], [1, 3]]), 1)\n2\n&gt;&gt;&gt; count_values(np.array([1, 2, 3]), 4)\n0\n&gt;&gt;&gt; count_values(np.array([]), 1.0)\n0\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.equal_to","title":"metriclab.utils.array.equal_to","text":"<pre><code>equal_to(arr: ndarray, value: Any) -&gt; ndarray\n</code></pre> <p>Return a boolean mask indicating which elements equal a given value.</p> <p>Works correctly with special values: <code>nan</code> (using :func:<code>numpy.isnan</code> since <code>nan != nan</code>), and <code>None</code> (using identity check in object arrays).</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>The input array.</p> required <code>value</code> <code>Any</code> <p>The value to compare against.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>A boolean array of the same shape as <code>arr</code> where <code>True</code> indicates the element equals <code>value</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import equal_to\n&gt;&gt;&gt; equal_to(np.array([1, 2, 3, 2]), 2)\narray([False,  True, False,  True])\n&gt;&gt;&gt; equal_to(np.array([1.0, float(\"nan\"), 3.0, float(\"nan\")]), float(\"nan\"))\narray([False,  True, False,  True])\n&gt;&gt;&gt; equal_to(np.array([1, None, 3, None], dtype=object), None)\narray([False,  True, False,  True])\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.is_nan","title":"metriclab.utils.array.is_nan","text":"<pre><code>is_nan(arr: ndarray) -&gt; ndarray\n</code></pre> <p>Test element-wise for NaN values and return result as a boolean array.</p> <p>A value is considered missing if it is <code>NaN</code>.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>The input array to test.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>A boolean array. <code>True</code> where the value is NaN, <code>False</code> otherwise.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import is_nan\n&gt;&gt;&gt; is_nan(np.array([1.0, 0.0, float(\"nan\"), 1.0]))\narray([False, False,  True, False])\n&gt;&gt;&gt; is_nan(np.array([1.0, 0.0, float(\"nan\"), 1.0, None], dtype=object))\narray([False, False,  True, False,  False])\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.multi_contains_value","title":"metriclab.utils.array.multi_contains_value","text":"<pre><code>multi_contains_value(\n    arrays: Sequence[ndarray], value: Any\n) -&gt; bool\n</code></pre> <p>Check if a value is present in any of the input numpy arrays.</p> <p>Works correctly with special values such as <code>nan</code>, <code>inf</code>, and <code>None</code>.</p> <p>Parameters:</p> Name Type Description Default <code>arrays</code> <code>Sequence[ndarray]</code> <p>The input arrays to search.</p> required <code>value</code> <code>Any</code> <p>The value to search for.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>value</code> is present in any array, <code>False</code> otherwise.</p> <code>bool</code> <p>Returns <code>False</code> when <code>arrays</code> is empty.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import multi_contains_value\n&gt;&gt;&gt; multi_contains_value([np.array([1, 0, 2, 1]), np.array([1, 3, 0, 1])], value=2)\nTrue\n&gt;&gt;&gt; multi_contains_value([np.array([1, 0, 0, 1]), np.array([1, 3, 0, 1])], value=2)\nFalse\n&gt;&gt;&gt; multi_contains_value(\n...     [np.array([1.0, 0.0, 0.0]), np.array([1.0, float(\"nan\"), 0.0])],\n...     value=float(\"nan\"),\n... )\nTrue\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.multi_count_unique_non_missing","title":"metriclab.utils.array.multi_count_unique_non_missing","text":"<pre><code>multi_count_unique_non_missing(\n    arrays: Sequence[ndarray], missing_values: Any = NOT_SET\n) -&gt; int\n</code></pre> <p>Count the number of unique non-missing values across all arrays.</p> <p>Counts unique values across all input arrays combined, as if all arrays were concatenated into one. Works correctly with special values such as <code>nan</code>, <code>inf</code>, and <code>None</code>.</p> <p>Parameters:</p> Name Type Description Default <code>arrays</code> <code>Sequence[ndarray]</code> <p>The input arrays to search.</p> required <code>missing_values</code> <code>Any</code> <p>The value representing missing data. If not set, all unique values are counted including <code>nan</code>, <code>inf</code>, and <code>None</code>.</p> <code>NOT_SET</code> <p>Returns:</p> Type Description <code>int</code> <p>The number of unique non-missing values across all arrays. Returns <code>0</code> when <code>arrays</code> is empty or all arrays are empty.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import multi_count_unique_non_missing\n&gt;&gt;&gt; # two arrays with overlapping values\n&gt;&gt;&gt; multi_count_unique_non_missing([np.array([1, 0, 2]), np.array([2, 3, 0])])\n4\n&gt;&gt;&gt; # nan excluded when missing_values=float(\"nan\")\n&gt;&gt;&gt; multi_count_unique_non_missing(\n...     [np.array([1.0, float(\"nan\")]), np.array([2.0, float(\"nan\")])],\n...     missing_values=float(\"nan\"),\n... )\n2\n&gt;&gt;&gt; # empty list of arrays\n&gt;&gt;&gt; multi_count_unique_non_missing([])\n0\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.multi_equal_to","title":"metriclab.utils.array.multi_equal_to","text":"<pre><code>multi_equal_to(\n    arrays: Sequence[ndarray], value: Any\n) -&gt; ndarray\n</code></pre> <p>Test element-wise equality against a value for all input arrays and return result as a boolean array.</p> <p>Returns <code>True</code> at positions where any array has an element equal to <code>value</code>. Works correctly with special values such as <code>nan</code>, <code>inf</code>, and <code>None</code>.</p> <p>Parameters:</p> Name Type Description Default <code>arrays</code> <code>Sequence[ndarray]</code> <p>The input arrays to test. All arrays must have the same shape.</p> required <code>value</code> <code>Any</code> <p>The value to test equality against.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>A boolean array of the same shape as the input arrays. <code>True</code> where any array has an element equal to <code>value</code>, <code>False</code> otherwise.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>arrays</code> is empty.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import multi_equal_to\n&gt;&gt;&gt; multi_equal_to([np.array([1, 0, 2, 1]), np.array([1, 2, 0, 1])], value=2)\narray([False,  True,  True, False])\n&gt;&gt;&gt; multi_equal_to(\n...     [np.array([1.0, 0.0, float(\"nan\")]), np.array([1.0, float(\"nan\"), 0.0])],\n...     value=float(\"nan\"),\n... )\narray([False,  True,  True])\n&gt;&gt;&gt; multi_equal_to(\n...     [np.array([1, None, 3], dtype=object), np.array([None, 2, 3], dtype=object)],\n...     value=None,\n... )\narray([ True,  True, False])\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.multi_is_nan","title":"metriclab.utils.array.multi_is_nan","text":"<pre><code>multi_is_nan(arrays: Sequence[ndarray]) -&gt; ndarray\n</code></pre> <p>Test element-wise for missing values for all input arrays and return result as a boolean array.</p> <p>A value is considered missing if it is <code>NaN</code> or <code>None</code>.</p> <p>Parameters:</p> Name Type Description Default <code>arrays</code> <code>Sequence[ndarray]</code> <p>The input arrays to test. All the arrays must have the same shape.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>A boolean array. <code>True</code> where any array has a missing value, <code>False</code> otherwise.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>arrays</code> is empty.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import multi_is_nan\n&gt;&gt;&gt; mask = multi_is_nan([np.array([1, 0, 0, 1, np.nan]), np.array([1, np.nan, 0, 1, 1])])\n&gt;&gt;&gt; mask\narray([False,  True, False, False,  True])\n&gt;&gt;&gt; mask = multi_is_nan(\n...     [np.array([1, np.nan, 0], dtype=object), np.array([None, 2, 0], dtype=object)]\n... )\n&gt;&gt;&gt; mask\narray([False,  True, False])\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.multi_unique","title":"metriclab.utils.array.multi_unique","text":"<pre><code>multi_unique(\n    arrays: Sequence[ndarray], missing_values: Any = NOT_SET\n) -&gt; ndarray\n</code></pre> <p>Return the sorted unique values across all arrays.</p> <p>Parameters:</p> Name Type Description Default <code>arrays</code> <code>Sequence[ndarray]</code> <p>A list of arrays to compute unique values across. Arrays may have different dtypes and lengths. Empty arrays contribute no values to the result. For arrays that can contain NaN (including object arrays holding <code>float('nan')</code>), multiple NaN values are collapsed to a single NaN at the end of the result.</p> required <code>missing_values</code> <code>Any</code> <p>Value representing missing data. If <code>NOT_SET</code>, all unique values are included. If set, occurrences of <code>missing_values</code> are excluded from the result.</p> <code>NOT_SET</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>A 1-D sorted array of unique values. Returns an empty array when <code>arrays</code> is empty or all values are filtered out.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; multi_unique([np.array([1, 3, 4, 3]), np.array([3, 1, 2, 1]), np.array([6, 3, 4, 2])])\narray([1, 2, 3, 4, 6])\n&gt;&gt;&gt; multi_unique([np.array([1.0, float(\"nan\")]), np.array([2.0, float(\"nan\")])])\narray([ 1.,  2., nan])\n&gt;&gt;&gt; multi_unique(\n...     [np.array([\"cat\", \"dog\"], dtype=object), np.array([\"dog\", \"bear\"], dtype=object)]\n... )\narray(['bear', 'cat', 'dog'], dtype=object)\n&gt;&gt;&gt; multi_unique(\n...     [\n...         np.array([float(\"nan\"), 1.0], dtype=object),\n...         np.array([float(\"nan\"), 2.0], dtype=object),\n...     ]\n... )\narray([1.0, 2.0, nan], dtype=object)\n&gt;&gt;&gt; multi_unique(\n...     [np.array([1.0, 2.0, float(\"nan\")]), np.array([2.0, 3.0])],\n...     missing_values=float(\"nan\"),\n... )\narray([1., 2., 3.])\n&gt;&gt;&gt; # all values filtered out\n&gt;&gt;&gt; multi_unique([np.array([float(\"nan\")])], missing_values=float(\"nan\"))\narray([], dtype=float64)\n&gt;&gt;&gt; multi_unique([])\narray([], dtype=float64)\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.preprocess_1d","title":"metriclab.utils.array.preprocess_1d","text":"<pre><code>preprocess_1d(\n    arrays: Sequence[ndarray],\n    missing_policy: MissingPolicy = \"propagate\",\n    missing_values: Any = NOT_SET,\n) -&gt; list[ndarray]\n</code></pre> <p>Preprocess a sequence of 1-dimensional arrays by optionally removing rows with a specific missing value.</p> <p>Parameters:</p> Name Type Description Default <code>arrays</code> <code>Sequence[ndarray]</code> <p>The arrays to preprocess. All arrays must be 1-dimensional and have the same shape.</p> required <code>missing_policy</code> <code>MissingPolicy</code> <p>The policy for handling missing values. Valid values are <code>'omit'</code>, <code>'propagate'</code>, or <code>'raise'</code>. Only <code>'omit'</code> has an effect \u2014 it removes rows where any array equals <code>missing_values</code>. <code>'raise'</code> raises a <code>ValueError</code> if any array contains <code>missing_values</code>. <code>'propagate'</code> leaves the arrays unchanged.</p> <code>'propagate'</code> <code>missing_values</code> <code>Any</code> <p>The value to treat as missing. Only used when <code>missing_policy</code> is <code>'omit'</code> or <code>'raise'</code>. If not set, no rows are removed regardless of <code>missing_policy</code>.</p> <code>NOT_SET</code> <p>Returns:</p> Type Description <code>list[ndarray]</code> <p>A list of preprocessed arrays with the same order as the input. Returns an empty list when <code>arrays</code> is empty.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>missing_policy</code> is invalid.</p> <code>ValueError</code> <p>if the arrays do not all have the same shape.</p> <code>ValueError</code> <p>if any array is not 1-dimensional.</p> <code>ValueError</code> <p>if any array contains <code>missing_values</code> and <code>missing_policy</code> is <code>'raise'</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import preprocess_1d\n&gt;&gt;&gt; arrays = [np.array([1, 0, 0, 1, 1, np.nan]), np.array([0, 1, 0, 1, np.nan, 1])]\n&gt;&gt;&gt; preprocess_1d(arrays)\n[array([ 1.,  0.,  0.,  1.,  1., nan]), array([ 0.,  1.,  0.,  1., nan,  1.])]\n&gt;&gt;&gt; preprocess_1d(arrays, missing_policy=\"omit\", missing_values=float(\"nan\"))\n[array([1., 0., 0., 1.]), array([0., 1., 0., 1.])]\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.remove_duplicate_nans","title":"metriclab.utils.array.remove_duplicate_nans","text":"<pre><code>remove_duplicate_nans(arr: ndarray) -&gt; ndarray\n</code></pre> <p>Return arr with duplicate NaN values collapsed to a single NaN appended at the end.</p> <p>A single NaN is preserved when present. Arrays with zero or one NaN are returned unchanged. All dtypes are handled safely: non-numeric arrays that cannot contain NaN are returned unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>A 1-D array to remove duplicate NaNs from.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>A 1-D array with at most one NaN value. When duplicates are removed, the single remaining NaN is placed at the end.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>arr</code> is not 1-D.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import remove_duplicate_nans\n&gt;&gt;&gt; remove_duplicate_nans(np.array([1.0, float(\"nan\"), 2.0, float(\"nan\")]))\narray([ 1.,  2., nan])\n&gt;&gt;&gt; remove_duplicate_nans(np.array([1.0, 2.0, 3.0]))\narray([1., 2., 3.])\n&gt;&gt;&gt; remove_duplicate_nans(np.array([\"cat\", \"dog\"], dtype=object))\narray(['cat', 'dog'], dtype=object)\n&gt;&gt;&gt; remove_duplicate_nans(np.array([]))\narray([], dtype=float64)\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.remove_nans","title":"metriclab.utils.array.remove_nans","text":"<pre><code>remove_nans(arr: ndarray) -&gt; ndarray\n</code></pre> <p>Return arr with all NaN values removed.</p> <p>All dtypes are handled safely: non-numeric arrays that cannot contain NaN are returned unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>A 1-D array to remove NaN values from.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>A 1-D array with no NaN values. Returns an empty array when all values are NaN or the input is empty.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>arr</code> is not 1-D.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; remove_nans(np.array([1.0, float(\"nan\"), 2.0, float(\"nan\")]))\narray([1., 2.])\n&gt;&gt;&gt; remove_nans(np.array([1.0, 2.0, 3.0]))\narray([1., 2., 3.])\n&gt;&gt;&gt; remove_nans(np.array([\"cat\", \"dog\"], dtype=object))\narray(['cat', 'dog'], dtype=object)\n&gt;&gt;&gt; remove_nans(np.array([float(\"nan\"), float(\"nan\")]))\narray([], dtype=float64)\n&gt;&gt;&gt; remove_nans(np.array([]))\narray([], dtype=float64)\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.remove_values","title":"metriclab.utils.array.remove_values","text":"<pre><code>remove_values(arr: ndarray, value: Any) -&gt; ndarray\n</code></pre> <p>Return arr with all occurrences of value removed.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>A 1-D array to remove values from.</p> required <code>value</code> <code>Any</code> <p>The value to remove from arr. If value is NaN, all NaN values are removed regardless of sign or dtype.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>A 1-D array with all occurrences of value removed. Returns an empty array when all values match or the input is empty.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>arr</code> is not 1-D.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; remove_values(np.array([1.0, 2.0, 1.0, 3.0]), 1.0)\narray([2., 3.])\n&gt;&gt;&gt; remove_values(np.array([1.0, float(\"nan\"), 2.0, float(\"nan\")]), float(\"nan\"))\narray([1., 2.])\n&gt;&gt;&gt; remove_values(np.array([\"cat\", \"dog\", \"cat\"], dtype=object), \"cat\")\narray(['dog'], dtype=object)\n&gt;&gt;&gt; remove_values(np.array([1.0, 2.0, 3.0]), 4.0)\narray([1., 2., 3.])\n&gt;&gt;&gt; remove_values(np.array([]), 1.0)\narray([], dtype=float64)\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.to_numpy","title":"metriclab.utils.array.to_numpy","text":"<pre><code>to_numpy(x: ArrayLike, name: str = 'input') -&gt; ndarray\n</code></pre> <p>Convert an array-like object to a NumPy array.</p> <p>Supported input types are :class:<code>numpy.ndarray</code>, :class:<code>polars.Series</code>, :class:<code>list</code>, and :class:<code>tuple</code>.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>The array-like object to convert.</p> required <code>name</code> <code>str</code> <p>The name of the input, used in error messages. Defaults to <code>\"input\"</code>.</p> <code>'input'</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>A NumPy array representation of <code>x</code>. If <code>x</code> is already a :class:<code>numpy.ndarray</code>, it is returned as-is without copying.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>x</code> is not a supported array-like type.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import to_numpy\n&gt;&gt;&gt; to_numpy([1, 2, 3])\narray([1, 2, 3])\n&gt;&gt;&gt; to_numpy((1, 2, 3))\narray([1, 2, 3])\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.to_numpy_1d","title":"metriclab.utils.array.to_numpy_1d","text":"<pre><code>to_numpy_1d(x: ArrayLike, name: str = 'input') -&gt; ndarray\n</code></pre> <p>Convert an array-like object to a 1D NumPy array.</p> <p>Supported input types are the same as :func:<code>to_numpy</code>.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>The array-like object to convert. Must be 1-dimensional after conversion.</p> required <code>name</code> <code>str</code> <p>The name of the input, used in error messages. Defaults to <code>\"input\"</code>.</p> <code>'input'</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>A 1D NumPy array representation of <code>x</code>.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>x</code> is not a supported array-like type.</p> <code>ValueError</code> <p>If <code>x</code> does not have exactly one dimension after conversion.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import to_numpy_1d\n&gt;&gt;&gt; to_numpy_1d([1, 2, 3])\narray([1, 2, 3])\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.validate_array_ndim","title":"metriclab.utils.array.validate_array_ndim","text":"<pre><code>validate_array_ndim(\n    arr: ndarray, ndim: int, name: str = \"input\"\n) -&gt; None\n</code></pre> <p>Validate that an array has the expected number of dimensions.</p> <p>Parameters:</p> Name Type Description Default <code>arr</code> <code>ndarray</code> <p>The array to validate.</p> required <code>ndim</code> <code>int</code> <p>The expected number of dimensions.</p> required <code>name</code> <code>str</code> <p>The name of the input, used in error messages. Defaults to <code>\"input\"</code>.</p> <code>'input'</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>arr.ndim</code> is different from <code>ndim</code>.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import validate_array_ndim\n&gt;&gt;&gt; validate_array_ndim(np.ones((2, 3)), ndim=2)\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.validate_nan_policy","title":"metriclab.utils.array.validate_nan_policy","text":"<pre><code>validate_nan_policy(nan_policy: str) -&gt; None\n</code></pre> <p>Validate a NaN handling policy value.</p> <p>Parameters:</p> Name Type Description Default <code>nan_policy</code> <code>str</code> <p>The policy name to validate.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if <code>nan_policy</code> is not <code>'omit'</code>, <code>'propagate'</code>, or <code>'raise'</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.array import validate_nan_policy\n&gt;&gt;&gt; validate_nan_policy(\"omit\")\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.array.validate_same_shape","title":"metriclab.utils.array.validate_same_shape","text":"<pre><code>validate_same_shape(arrays: Iterable[ndarray]) -&gt; None\n</code></pre> <p>Check if arrays have the same shape.</p> <p>Parameters:</p> Name Type Description Default <code>arrays</code> <code>Iterable[ndarray]</code> <p>The arrays to check.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>if the arrays have different shapes.</p> Example <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from metriclab.utils.array import validate_same_shape\n&gt;&gt;&gt; validate_same_shape([np.array([1, 0, 0, 1]), np.array([0, 1, 0, 1])])\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.imports","title":"metriclab.utils.imports","text":"<p>Helpers for optional dependencies used by metriclab.</p> <p>The utilities in this package let callers:</p> <ul> <li>check whether an optional package is installed,</li> <li>fail early with a clear error when a package is required,</li> <li>gate function execution behind package availability.</li> </ul>"},{"location":"refs/utils/#metriclab.utils.imports.check_colorlog","title":"metriclab.utils.imports.check_colorlog","text":"<pre><code>check_colorlog() -&gt; None\n</code></pre> <p>Check if the <code>colorlog</code> package is installed.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>if the <code>colorlog</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.imports import check_colorlog\n&gt;&gt;&gt; try:\n...     check_colorlog()\n... except RuntimeError:\n...     pass\n...\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.imports.check_rich","title":"metriclab.utils.imports.check_rich","text":"<pre><code>check_rich() -&gt; None\n</code></pre> <p>Check if the <code>rich</code> package is installed.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>if the <code>rich</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.imports import check_rich\n&gt;&gt;&gt; try:\n...     check_rich()\n... except RuntimeError:\n...     pass\n...\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.imports.colorlog_available","title":"metriclab.utils.imports.colorlog_available","text":"<pre><code>colorlog_available(fn: F) -&gt; F\n</code></pre> <p>Implement a decorator to execute a function only if <code>colorlog</code> is installed.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>F</code> <p>The function to conditionally execute.</p> required <p>Returns:</p> Type Description <code>F</code> <p>A wrapper around <code>fn</code>. When <code>colorlog</code> is unavailable, calling the wrapper returns <code>None</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.imports import colorlog_available\n&gt;&gt;&gt; @colorlog_available\n... def my_function(n: int = 0) -&gt; int:\n...     return 42 + n\n...\n&gt;&gt;&gt; my_function()\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.imports.is_colorlog_available","title":"metriclab.utils.imports.is_colorlog_available  <code>cached</code>","text":"<pre><code>is_colorlog_available() -&gt; bool\n</code></pre> <p>Indicate if the <code>colorlog</code> package is installed or not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>colorlog</code> is available otherwise <code>False</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.imports import is_colorlog_available\n&gt;&gt;&gt; is_colorlog_available()\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.imports.is_rich_available","title":"metriclab.utils.imports.is_rich_available  <code>cached</code>","text":"<pre><code>is_rich_available() -&gt; bool\n</code></pre> <p>Indicate if the <code>rich</code> package is installed or not.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if <code>rich</code> is available otherwise <code>False</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.imports import is_rich_available\n&gt;&gt;&gt; is_rich_available()\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.imports.raise_colorlog_missing_error","title":"metriclab.utils.imports.raise_colorlog_missing_error","text":"<pre><code>raise_colorlog_missing_error() -&gt; NoReturn\n</code></pre> <p>Raise a <code>RuntimeError</code> to indicate the <code>colorlog</code> package is missing.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Always, with a message indicating that the <code>colorlog</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.imports import raise_colorlog_missing_error\n&gt;&gt;&gt; try:\n...     raise_colorlog_missing_error()\n... except RuntimeError as e:\n...     \"'colorlog' package is required\" in str(e)\n...\nTrue\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.imports.raise_rich_missing_error","title":"metriclab.utils.imports.raise_rich_missing_error","text":"<pre><code>raise_rich_missing_error() -&gt; NoReturn\n</code></pre> <p>Raise a <code>RuntimeError</code> to indicate the <code>rich</code> package is missing.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Always, with a message indicating that the <code>rich</code> package is not installed.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.imports import raise_rich_missing_error\n&gt;&gt;&gt; try:\n...     raise_rich_missing_error()\n... except RuntimeError as e:\n...     \"'rich' package is required\" in str(e)\n...\nTrue\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.imports.rich_available","title":"metriclab.utils.imports.rich_available","text":"<pre><code>rich_available(fn: F) -&gt; F\n</code></pre> <p>Implement a decorator to execute a function only if <code>rich</code> is installed.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>F</code> <p>The function to conditionally execute.</p> required <p>Returns:</p> Type Description <code>F</code> <p>A wrapper around <code>fn</code>. When <code>rich</code> is unavailable, calling the wrapper returns <code>None</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.imports import rich_available\n&gt;&gt;&gt; @rich_available\n... def my_function(n: int = 0) -&gt; int:\n...     return 42 + n\n...\n&gt;&gt;&gt; my_function()\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.logging","title":"metriclab.utils.logging","text":"<p>Helpers to configure and format logging output.</p>"},{"location":"refs/utils/#metriclab.utils.logging.configure_logging","title":"metriclab.utils.logging.configure_logging","text":"<pre><code>configure_logging(level: int = INFO) -&gt; None\n</code></pre> <p>Configure the logging module with a colored formatter.</p> <p>If the <code>colorlog</code> package is installed, a colored formatter is used. Otherwise, the standard <code>logging.basicConfig</code> is called.</p> <p>Parameters:</p> Name Type Description Default <code>level</code> <code>int</code> <p>The minimum log level to capture. Defaults to <code>logging.INFO</code>.</p> <code>INFO</code> Example <pre><code>&gt;&gt;&gt; import logging\n&gt;&gt;&gt; from metriclab.utils.logging import configure_logging\n&gt;&gt;&gt; configure_logging(level=logging.DEBUG)\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.logging.log_dict_pretty","title":"metriclab.utils.logging.log_dict_pretty","text":"<pre><code>log_dict_pretty(\n    data: dict[Any, Any],\n    level: int = INFO,\n    title: str | None = None,\n) -&gt; None\n</code></pre> <p>Log a dictionary in a pretty format if rich is available.</p> <p>If the <code>rich</code> package is installed, the dictionary is rendered using :class:<code>~rich.pretty.Pretty</code> and printed to the console via :class:<code>~rich.console.Console</code>. Otherwise, the dictionary is logged with the standard :mod:<code>logging</code> module at the specified level.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[Any, Any]</code> <p>The dictionary to log.</p> required <code>level</code> <code>int</code> <p>The log level used when <code>rich</code> is not available. Defaults to <code>logging.INFO</code>.</p> <code>INFO</code> <code>title</code> <code>str | None</code> <p>Optional panel title when <code>rich</code> is available, and a text prefix when <code>rich</code> is unavailable.</p> <code>None</code> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.logging import log_dict_pretty\n&gt;&gt;&gt; log_dict_pretty({\"accuracy\": 0.75}, title=\"Validation\")\n</code></pre>"},{"location":"refs/utils/#metriclab.utils.missing","title":"metriclab.utils.missing","text":"<p>Validation helpers for missing-value handling policies.</p>"},{"location":"refs/utils/#metriclab.utils.missing.validate_missing_policy","title":"metriclab.utils.missing.validate_missing_policy","text":"<pre><code>validate_missing_policy(missing_policy: str) -&gt; None\n</code></pre> <p>Validate a missing-value policy value.</p> <p>Parameters:</p> Name Type Description Default <code>missing_policy</code> <code>str</code> <p>The policy name to validate.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>missing_policy</code> is not one of :obj:<code>metriclab.utils.missing.MISSING_POLICIES</code>.</p> Example <pre><code>&gt;&gt;&gt; from metriclab.utils.missing import validate_missing_policy\n&gt;&gt;&gt; validate_missing_policy(\"omit\")\n</code></pre>"}]}